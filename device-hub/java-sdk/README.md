# Java 21 SDK

Dependency-free normalized contracts plus generic HTTP probe adapters. Use the Python gateway for full configuration-driven routing, or embed these contracts in a Java ERP. Hikvision ISAPI digest authentication should be handled by your application HTTP stack or sidecar; the sample Java adapter intentionally does not fake Digest auth.
