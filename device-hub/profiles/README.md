# Device matrix

Rows are routing seeds, **not blanket hardware certification**. Add exact model/firmware rows during deployment. `SEED` means the integration route is architecturally supported but that exact hardware/firmware combination has not been tested in this repository.
