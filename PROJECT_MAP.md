# PROJECT_MAP.md - Bemo ERP

## System Flow & Architecture
- **Backend (`be/`)**: Spring Boot, PostgreSQL, App-Scoped JWT Auth, Multi-Role Authorization, Multi-Tenant (`@TenantId`), app-aware database i18n (`/api/v1/i18n/{locale}`) with NULL-app defaults, per-application overrides, and a Super Admin management API/view.
- **Frontend (`fe/`)**: Angular 22 standalone, SCSS, Signals, Arabic RTL default (`ar-EG`/`en-US`), Excel exports.

---

## [COMPLETED & VERIFIED]
- **Consolidated UI/UX Audit, Global Shortcut Gate & Platform Polish (Session 13)**: Complete resolution of source/UI-UX audit across global shell, keyboard shortcuts, information architecture, forms, tables, and settings. Unified all keyboard shortcuts (`Ctrl/Cmd+K`, `/`, `?`, `G → X`, `Esc`) through `shortcut-guard.util.ts` with strict modal suppression via `DialogStateService.modalOpen()`. Added dirty tracking (`hasUnsavedChanges`) and discard workflow in `ShortcutSettingsComponent`, aligned shortcut key codes (`KeyA..KeyZ` + `Digit0..Digit9`), un-nested interactive buttons from navigation links, expanded `IconComponent` with 11 SVG icons (`star`, `clock`, `bell`, `chat`, `wallet`, `cart`, `boxes`, `banknote`, `building`, `factory`, `search`) to eliminate raw emoji in shell chrome, configured clean default collapsed state for secondary/vertical modules, converted employee form accordion to button-driven semantics, refactored journal entry lines editor with analytical dimension sub-rows, added disabled tooltip guidance for employee creation without categories, and added mobile responsive layouts across tables and toolbars. Verified 100% clean with 643 frontend unit tests, 5,851 database i18n keys, 0 hardcoded violations, and full backend test execution (`BUILD SUCCESSFUL`).
- **API Authorization & Role Security Enforcements (P0-01)**: Enforced `@PreAuthorize` role rules across all Spring Boot controllers. Prevented unauthorized execution of disbursement, posting, and user updates at API layer (returns `403 Forbidden`).
- **Security Pack: 2FA TOTP, Password Policy, Trusted Devices & Role IP Allowlists (WP-33)**: Built enterprise security suite with RFC 6238 TOTP two-factor authentication, encrypted secrets at rest with AES-GCM, single-use emergency backup codes, tenant password complexity and history enforcement, trusted devices tracking with remote session revocation (`tv` bump), and role-based CIDR IP allowlists with Super Admin bypass. Liquibase `v442` (schema) and `v443` (translations). Backend domain package `com.bemo.hr.security.pack` with `TotpService`, `PasswordPolicyService`, `TrustedDeviceService`, `IpAllowlistService`, `SecurityPackController`, and `Auth2FaController`. Frontend `SecuritySettingsComponent` in `/settings` (Security tab) and 2FA challenge flow in `LoginPage`.
- **Fleet & Equipment Maintenance Hub (WP-44)**: Comprehensive vehicle registry linked to fixed assets, fuel logs with consecutive odometer consumption efficiency tracking (km/L), dual maintenance schedule due engine (by kilometers and elapsed calendar days) with auto-reset baseline, vehicle document renewals (license, insurance, inspection, permits) with lead-window expiry warnings, and fleet-wide cost report KPI aggregation (fuel + maintenance per vehicle and per km). Liquibase `v446` (schema) and `v447` (translations). Backend package `com.bemo.hr.fleet` with `Vehicle`, `FuelLog`, `MaintenanceSchedule`, `MaintenanceRecord`, `VehicleDocument`, `FleetService`, and `FleetController`. Frontend `features/fleet/` page (`/fleet`) with tabbed registry, quick modals, and KPI summary cards.
- **Employee Self-Service (ESS) Mobile Surfaces (WP-49)**: Empowered employees to directly track their employment profile, remaining annual leave balances, frozen payslip snapshots with detailed allowance/deduction/advances calculation formulas, submit leave requests with live balance and date-overlap validations, and submit loan/salary advance requests with customizable installment schedules. Liquibase `v448` (`app_users.employee_id` link) and `v449` (translations). Backend package `com.bemo.hr.ess` with `EssService` (strict user-ownership boundary) and `EssController` (`/api/v1/ess/*`). Frontend `features/self-service/` page (`/self-service`) with Overview, Payslips, Leaves, Advances, and Attendance tabs.
- **AI Intelligence & Decision Support Pack (WP-52)**: Complete predictive and anomaly detection decision support layer. Includes deterministic 12-month trailing moving average cash-flow forecast with expanding confidence bands, leave-one-out rolling historical baseline expense anomaly detector flagging transactions $> 2.5\sigma$, inventory demand forecast with lead-time buffer and suggested reorder quantities, customer collections risk scoring (Bands A/B/C) with transparent explainability factors, and natural language analytics query parser with strict descriptor whitelist validation. Zero mutating database writes. Liquibase `v450` translations. Backend package `com.bemo.hr.analytics.ai` with `AiIntelligenceService` and `AiIntelligenceController` (`/api/v1/analytics/*`). Frontend `features/ai-intelligence/` page (`/ai-intelligence`) with tabbed dashboard, interactive Q&A box, and KPI tables.


- **Contractor Workforce Financial Lifecycle (Phase 1 / Epic 1)**: Implemented complete connected financial lifecycle for contractor labor (Labor Request → Worker Assignment → Attendance Lock → 15-Day Settlement Period → Advances & Deductions → Settlement Review & Approval → Finance Journal Posting & Partner Ledger → Supplier Invoice Link → Payment & Disbursal). Added Liquibase V104 schema (`contractor_settlement_lines`, `contractor_settlement_adjustments`, versioning, invoice tracking) and V105 bilingual translations. Added `ContractorSettlementLine` & `ContractorSettlementAdjustment` entities, `WorkforceSettlementService` idempotency posting (`operationId`) with credit/debit partner ledger entries (`PartnerLedgerEntry`), and REST endpoints. Frontend updated with `ContractorSettlementDetailModalComponent` drawer for inspecting worker wage lines, linking invoices, posting to finance, and recording payments.
- **Configurable Approval Workflow Engine (Phase 1 / Epic 2)**: Developed reusable cross-module approval workflow engine supporting dynamic multi-step approval definitions, step role/user criteria, minimum approvals, self-approval policy checks, mandatory rejection comment enforcement, and decision audit logs. Liquibase V106 schema (`approval_workflow_definitions`, `approval_workflow_steps`, `approval_instances`, `approval_decisions`) and V107 bilingual translations. Backend domain package `com.bemo.hr.approval` with `ApprovalWorkflowService` and `@PreAuthorize` REST endpoints in `ApprovalController`. Frontend updated with `PendingApprovalsComponent` task drawer, `WorkflowDefinitionsComponent` builder, and `ApprovalService`.
- **Payroll Automation & Explanation (Phase 1 / Epic 3)**: Developed transparent formula breakdown engine for employee salary payments. Created `SalaryPaymentExplanation` entity, V108 Liquibase schema (`salary_payment_explanations`), V109 translations, `getPaymentExplanation` endpoint in `PayrollController`, `PayrollStore` methods, and interactive "💡 تفاصيل وحساب الراتب" calculation explanation modal in Angular `PayrollPage`.
- **Procurement Three-Way Matching (Phase 1 / Epic 4)**: Built automated PO / GRN / Supplier Invoice 3-way matching engine. Created `ProcurementThreeWayMatch` entity, V110 Liquibase schema (`procurement_three_way_matches`), V111 translations, price/quantity variance tolerance validation in `ProcurementService`, resolution endpoints in `ProcurementController`, 3-Way Match service unit suite, and interactive 3-Way Match badge & Resolution Modal in Angular `ProcurementPage`.
- **Action Center & Business Notifications (Phase 1 / Epic 5)**: Created enterprise Action Center and real-time notification engine. Created `BusinessNotification` entity, V112 Liquibase schema (`business_notifications`), V113 translations, `NotificationService`, REST controller endpoints (`/api/v1/notifications`), unit test suite, signal-based Angular `NotificationCenterService`, top header navbar bell button `🔔` with unread badge count, and Action Center dropdown drawer in `app-shell.component.ts`.
- **Dynamic Permission-Aware Screen Shortcuts (UX-SHORTCUTS-001)**: Implemented per-user, tenant-scoped dynamic screen navigation shortcuts saved per account (`user_shortcut_profiles` and `user_screen_shortcuts`). Physical `KeyboardEvent.code` key matching (`KeyH`, `Digit1`) guarantees 100% layout-independent shortcut execution across Arabic, English, and non-Latin input methods. Destination availability is strictly permission-aware (role codes, allowed menus, tenant feature flags, and canonical access catalog pages) and shortcuts never override Angular guards or backend `@PreAuthorize` rules. Saved shortcuts switch to status (`NO_ROLE`, `MENU_NOT_ALLOWED`, `FEATURE_DISABLED`, `PAGE_REMOVED`, `DISABLED`) rather than silent deletion. Includes validation (duplicate key `SHORTCUT_KEY_DUPLICATE`, duplicate destination `SHORTCUT_DESTINATION_DUPLICATE`, unknown page `SHORTCUT_PAGE_UNKNOWN`, unavailable page `SHORTCUT_PAGE_NOT_AVAILABLE`, limit > 20 `SHORTCUT_LIMIT_EXCEEDED`), optimistic locking `@Version` with `SHORTCUT_PROFILE_VERSION_CONFLICT` (HTTP 409), audit logging (`USER_SHORTCUTS_UPDATE`, `USER_SHORTCUTS_RESET`), cross-tab synchronization via `BroadcastChannel('bemo-screen-shortcuts')`, and complete settings management UI (`ShortcutSettingsComponent`). Liquibase V114 schema and V115 bilingual translations.
- **Loading State & Hydration UX (P1-01, QA-015, QA-016)**: Added skeleton loaders during cold loads on `/`, `/dashboard`, `/reports`, eliminating premature zero-data empty state flashes.
- **Payroll Lifecycle & State Guardrails (P1-02)**: Blocked individual and bulk salary disbursement at both API service layer and UI components unless the payroll cycle is in `APPROVED` or `POSTED` status. Converted epoch-millisecond dates into localized human-readable dates.
- **Immutable Append-Only Audit Logging (P1-03)**: Connected `AuditService.record()` across user authentication, payroll state transitions, report approvals/reopens, inventory movements, and settings changes.
- **Linked ERP Transactions & Hierarchy (P1-04)**: Added tenant-scoped company, branch, warehouse, and department APIs. Read operations are side-effect free; production organization data is created explicitly by authorized users, while sample organization/fiscal data lives only in the test changelog.
- **QA Defects & UX Enhancements**:
  - **QA-001**: Standardized empty report status UI to "لا توجد سجلات / لا ينطبق" when 0 records exist.
  - **QA-004**: Enforced mandatory biometric device ID field for active biometric employees with inline Arabic validation message.
  - **QA-005**: Standardized inventory movement quantity inputs to positive numbers, deriving direction (issue vs receipt) from movement type.
  - **QA-007**: Synchronized dashboard year switch (2025 vs 2026) with summary cards, canceled stale HTTP requests, and preserved URL query params.
  - **QA-008 & QA-012**: Corrected employee `001` name to `محمد أحمد علي`. Replaced bare action icon buttons (`✎`, `×`) with explicit text labels, tooltips, and keyboard accessibility.
  - **QA-011**: Added missing translation keys (`payroll.maxAdvanceDeduction`, `reports.emptyNoRecords`, etc.) and ensured instant reactive locale toggle.
- **Liquibase Database Migration (`v38`)**: Production V38 now contains only mandatory translation rows and required schema fields. Its former DEMO organization, currency, employee-name, and QA-cleanup records are isolated in the test-only changelog.
- **Attendance Report Bulk Decisions (P0)**: Implemented backend bulk-decision API with operation ID for idempotency (`POST /api/v1/reports/{id}/bulk-decision`), new `AttendanceDecision` types (`ABSENCE`, `OFFICIAL_HOLIDAY`, `INDIVIDUAL_REVIEW`), device/power-outage decision persistence (`PUT /api/v1/reports/{id}/downtime-decision`). Frontend enhanced filter panel (date range, category, attendance condition, review status), recommendation counts computed from filtered data, downtime decisions saved to backend with non-blocking feedback.
- **Business Parties / Supplier Relationship Model (P0)**: Added `nameEn`, `email`, `address`, `relationshipStartDate`, `relationshipEndDate` fields to `BusinessParty` entity. Updated API DTOs with validation (`@Email`, `@Pattern` for phone/tax ID). Enforced managed-type (`DIRECT`/`MANAGED`) with responsible-party requirement for managed suppliers. Added `POST /api/v1/parties/cleanup-phone` admin endpoint. Production V44 contains the schema and generic managed-type normalization; the known invalid QA phone cleanup is test-only. Frontend form updated with new fields, validation errors, conditional date pickers for managed suppliers, and i18n keys.
- **Bilingual Translation Catalog (P1)**: Added Liquibase V52 with the 508 missing Arabic/English database rows. All 714 literal frontend keys now pass `npm run check:i18n`, and the Angular Vitest target is runnable locally and in CI.
- **Procurement Document Flow (P0)**: Implemented the connected PO/GRN/invoice/payment pipeline. PO totals are derived from validated inventory-linked lines. GRNs support partial receiving, delivered/rejected/quality-deducted quantities, warehouse location and lot trace, block over-receipt, and post only accepted quantity to inventory. Supplier invoices preserve PO, GRN, supplier, and responsible-party trace; direct/managed invoice rules are enforced. Partial and multiple payments update outstanding balances and the partner ledger. A real multi-sheet `.xlsx` export is available. Liquibase V45, V49, and V50 carry the document, trace, and inventory-receiving schema.
- **Procurement Entry UX & Configurable Numbering (P0)**: Added tenant settings for either server-generated locked numeric PO/GRN sequences or unique manual numbers. Automatic numbering continues after each company's existing highest document number. PO dates accept historical/current/future values, draft orders can update supplier/date/item/quantity/price, line units come from the inventory master, and eligible draft/issued/partially received orders are selectable with their lines in GRN entry. Supplier selection is restricted to active registered suppliers. Liquibase V53 carries the setting, sequence storage, uniqueness rules, and Arabic/English UI copy.
- **Keyboard, Navigation & Import UX (P1)**: Added form Enter-to-submit, modal Escape-to-cancel, predictable Tab focus, focus-on-open, sidebar expand-all/collapse-all controls, Arabic import completion states, and more robust Excel date parsing including unformatted serial dates and common day/month/year formats.
- **Advanced Keyboard Navigation & Tooltips (P1)**: Added permission-aware `Ctrl+K` quick navigation with search and arrow/Enter selection, `?` shortcut help, `G`-then-letter direct menu chords, a dedicated settings reference tab, and descriptive tooltips carrying shortcut hints across shell navigation and controls. Liquibase V55 carries the Arabic/English copy.
- **Dynamic Dashboard Customization & Motion (P1)**: Added server-backed per-user widget visibility/order and motion preferences, per-user admin permission switches, a Super Admin-only tenant policy for Admin accounts, richer attendance/payroll/department charts, reduced-motion support, and Liquibase V56 bilingual copy.
- **Procurement Totals, Currency & Invoice Evidence (P0)**: Fixed live PO total recalculation and save enablement, added validated transaction currency defaulted from the supplier, separated nullable supplier invoice numbers from internal references, and made delivered/rejected/deducted/accepted receipt quantities explicit. Liquibase V57 carries the schema.
- **Workforce Reliability & Analysis (P0/P1)**: Fixed the hidden manual-attendance matrix, added analytical workforce charts honoring the motion preference, real Excel exports for contractors/workers/categories, and tenant advance policies with global/category/worker precedence. Liquibase V58 carries advance policies.
- **Unified Tooltip Accessibility (P1)**: Standardized 350ms hover/focus delay, two-line RTL surfaces, Escape dismissal, `aria-describedby`, and icon-only `aria-label` coverage in the shell and audited controls; added financial and bulk-action context help.
- **GRN Reliability & Validation (P0)**: Fixed goods-receipt line persistence, server-calculated remaining PO quantities, partial/full PO transitions, accepted-quantity inventory posting, negative/over-receipt validation, explicit Arabic UI feedback, decimal quantity steps, and the responsive accessible GRN dialog.
- **Live Biometric Device Integration (P0)**: Added tenant-owned IP/API device connections, immediate and scheduled synchronization, durable status/cursor state, batch and punch idempotency, identity matching, audit logging, and an Arabic responsive management UI alongside CSV/XLS/XLSX fallback.
- **Production/Test Bootstrap Boundary (P0)**: Production always installs translations and platform roles, then idempotently ensures configured Admin and Super Admin accounts. Demo organization, attendance, currency, supplier, inventory, and legacy QA correction data is reachable only through the test changelog or an explicitly enabled `dev`/`demo` profile.
- **Deployment & PostgreSQL Verification Hardening (P0/P1)**: Production Compose clears inherited development ports with `!reset []` so PostgreSQL and the backend are never published to the host in production; CI renders the merged production Compose and asserts private ports are absent while the frontend stays public. Backend build/CI/container execution is standardized on Temurin Java 21, with Java 17 bytecode/API compatibility enforced by `options.release = 17`. Supplier-payment overpayment concurrency is verified against real PostgreSQL via Testcontainers (`PostgresIntegrationTest` uses the full production Liquibase changelog + `ddl-auto=validate`, `@RepeatedTest(10)` with a thread-safe operation-ID list). This surfaced and fixed two fresh-deploy blockers in the production migrations: V80 `addUniqueConstraint` used YAML list `columnNames` (Liquibase requires a comma-separated string), and the V81 translation CSV had an unquoted embedded semicolon.
- **P0-08 (deferred, external)**: GitHub Actions still requires resolving the account/billing lock before the three CI jobs can produce independent evidence.
- **Technical-Review Hardening Batch (P0)**:
  - **P0-1/P0-2/P0-4 — Tenant & Evidence Integrity**: Tenant-owned aggregates keep `@TenantId` app scoping with immutable audit timestamps on evidence; shared idempotency (`operationId`) handling verified across bulk decisions, payroll transitions, and supplier payments; API dates remain epoch-milliseconds.
  - **P0-3 — Role-Based Security Parity**: Every domain controller now carries explicit `@PreAuthorize` sets matching backend role codes. Finance (`FINANCE_MANAGER`, `ACCOUNTANT`, `TREASURY_USER`, `HR_MANAGER`, `AUDITOR`), procurement (readers include `PROCUREMENT_USER`, `INVENTORY_MANAGER`; writes restricted to `PROCUREMENT_MANAGER`), sales/manufacturing/quality/payroll sets enforced; HR-domain controllers intentionally keep HR/ADMIN roles. Violations return `403`.
  - **P0-5 — Finance Journal State Machine**: Journal entries expose explicit state (`DRAFT`/`POSTED`/`REVERSED`) with guarded post/reverse transitions, and fiscal periods enforce open/closed lifecycle (`FiscalPeriodGuard`) so posting into a closed period is blocked.
  - **P0-6 — Fiscal Close Enforcement in Procurement**: `ProcurementService` now applies `FiscalPeriodGuard` to supplier-invoice and payment posting dates, preventing documents dated inside closed periods.
  - **P0-7 — Optimistic Locking**: `@Version` optimistic locking added to 14 mutable aggregates (PurchaseOrder, GoodsReceipt, SupplierInvoice, SupplierPayment, SalesOrder, ProductionOrder, QualityInspection, WorkforceSettlementPeriod, WorkforceAdvance, ManualAttendanceEntry, LaborRequest, BankAccount, Currency, TaxRate) with Liquibase V84 columns; concurrent edits now fail with a stale-state conflict.
  - **Backend Test Suite 108/108 Green**: Fixed a stale desktop `SpaForwardController` duplicate, corrected the V82 FK to reference the actual `apps` table, replaced a broken `@Lock` derived query with an explicit `@Query`, aligned `SecurityConfig`/access-denied handler with Jackson 3 (`tools.jackson.databind`), and made supplier-payment concurrency tests seed an open fiscal period. Verified against both H2 and PostgreSQL Testcontainers (full production Liquibase changelog + `ddl-auto=validate`).
  - **Frontend Session & Role Hardening**: Access tokens live in memory only (persisted session stores metadata, never the token) with single-flight refresh-on-401 retry and startup rehydration; `RoleCode` extended with the 14 domain roles; forced-password-change flow (`POST /auth/change-password`) with a dedicated change-password page, route, and i18n (V85 translations); workforce menu bypass removed and domain route/nav guards mirror the backend role sets; user-management role options and role labels cover all domain roles.
  - **Workforce Import Safety (Guide 07 / PR B)**: Backend import hardening for the reverse/validate/preview pipeline. Bounded queries on reverse, validate, and preview (reverse bulk-restores only the rows of the target batch, validate batches by normalized code, preview caps the returned rows at the configured limit). Hard limits: 20 MB max upload file (`EXCEL_FILE_TOO_LARGE`) and 20 000 max rows (`EXCEL_TOO_MANY_ROWS`), both env-overridable via `HR_WORKFORCE_IMPORT_MAX_FILE_BYTES` / `HR_WORKFORCE_IMPORT_MAX_ROWS`, plus `HR_WORKFORCE_IMPORT_PREVIEW_LIMIT` (100) for the bounded preview. All `BusinessRuleException`s carry stable machine keys (`WORKFORCE_IMPORT_*`, `EXCEL_*`) so responses are DB-localized (`ApiExceptionHandler` resolves by key with the constructor message as fallback); static Arabic messages were removed in favor of Liquibase V88 translation rows (17 keys × ar-EG/en-US). `WorkforceExcelImportService` is fully covered by a 13-case unit suite. Backend build now targets Java 17 (`options.release = 17`) while the Docker images still run Temurin Java 26; all `getFirst()`/`getLast()` calls were converted to Java 17 equivalents.
  - **Auth Review Close (Guide 06)**: New `AuthSecurityIntegrationTests` suite (11 `@SpringBootTest` cases) proves account lockout after 5 failures (`LoginStateService`), unknown-app/unknown-user throttling with global-IP fallback (`LoginRateLimiter`), refresh-token rotation invalidating the previous token with family-reuse revocation and bounded 7-day cleanup (`RefreshTokenService`), logout revocation, forced-password change clearing `mustChangePassword`, Super Admin-only dashboard-customization policy, workforce (`WORKFORCE_MANAGER`/`WORKFORCE_REVIEWER`/etc.) and admin (`SUPER_ADMIN`/`ADMIN`) endpoint role enforcement over real MockMvc HTTP, and the V83 corrective migration granting `HR_MANAGER` only `WORKFORCE_MANAGER` + `WORKFORCE_REVIEWER` (no auto payroll/finance grants). `RevocableJwtAuthenticationConverter` now reads the `tv` claim as `Number`, fixing a Jackson-3 Long-decode `ClassCastException` that previously 500'd authenticated requests. Frontend auth calls send `withCredentials`, `logout()` always posts to `/api/v1/auth/logout` before clearing session state, and a `workforceRoleGuard` protects all workforce routes.
  - **Workforce Import Commit Concurrency (Release Hardening)**: `WorkforceImportBatch` now carries `@Version` and the commit path reloads the batch under a pessimistic write lock (`WorkforceImportBatchRepository.findByIdForUpdate`), so two concurrent requests sharing one `operationId` produce exactly one applied import and one idempotent replay. Liquibase V89 adds the `version` column; `WorkforceImportCommitConcurrencyTests` (`@PostgresIntegrationTest`, `@RepeatedTest(10)`) asserts `applied==1`, `replayed==1`, one attendance row, and one import change. Runs in CI/Testcontainers (no Docker locally).
- **Punch Device-Scoped Dedup (Release Hardening)**: `PunchRecord` gained `device_id` and punch idempotency is now per `(app_id, device_id, device_user_id, punched_at)` via Liquibase V90's unique index `uq_punch_records_app_device_user_time`, so the same punch reported by one device is stored exactly once even across concurrent syncs (file imports pass a `null` device id). Liquibase V90 mirrored into the H2 test changelog; `attendance.*` and demo-data H2 tests green.
- **Backend Error i18n Sweep (Release Hardening)**: Every static `BusinessRuleException` and `NotFoundException` message across 20+ services now carries a stable machine key; `NotFoundException` gained an optional `(message, code)` constructor resolved by `ApiExceptionHandler` exactly like business rules. Concatenated/dynamic messages intentionally remain single-arg so their runtime fallback survives. Liquibase V91 loads 170 keys × ar-EG/en-US (340 rows), registered in `next.changelog-master.yaml` and mirrored in the H2 suite. Verified: backend compiles and all 145 runnable H2 tests pass (the only 2 failures are Docker-dependent concurrency suites).
- **Punch Source Identity & Import Evidence (P0)**: Every punch record, import batch, and upload now carries a tenant-owned `biometric_sources` identity (`id`, `app_id`, `source_type DEVICE|FILE_DEVICE`, `name`, `normalized_code`, `active`). Punches are unique per `(app_id, source_id, device_user_id, punched_at)` and batches per `(app_id, source_id, checksum)`, replacing the old device-scoped dedup. Uploads take `sourceId`; a `punch_import_evidence` audit table stores per-punch `(punch_id, batch_id, app_id, row_number, raw_line)` so an import can always be traced back to its source file. Liquibase V92 rewrites the schema (multi-row backfill of legacy punches to a `FILE_DEVICE` source, evidence backfill, dedupe, NOT NULL, source-scoped uniqueness) with an H2 mirror. Backend adds `BiometricSource`/`PunchImportEvidence` entities, repos, source CRUD endpoints, and `BiometricImportService`/`BiometricDeviceSyncService` rewritten for sourceId + evidence. Frontend imports page offers source management (create/edit/delete, active toggle) and a source selector on the upload dropzone; batches show their source. Liquibase V95 loads the 17 new bilingual keys.
- **Idempotency Owner Lease & Heartbeat (P0)**: Shared idempotency reservations now use an owner-token + attempt-number + heartbeatable lease (V93/V94). `reserve`, `steal`, `renewLease`, `complete`, and `fail` all run `REQUIRES_NEW`; the request-hash is re-checked before any status report so a stale owner can never read or mutate a reservation it no longer holds. `IdempotencyServiceTests` covers hash mismatch, stolen-lease races, and retry semantics.
- **Super Admin Authorization Parity (P1-05, resolved)**: No `RoleHierarchy` bean was introduced. Instead, `SUPER_ADMIN` was added explicitly to every `@PreAuthorize` expression that previously admitted `ADMIN` but omitted it — ReportController (preview, decision-history, create, bulk-decision, daily/downtime/anomaly/holiday decisions, approve, reopen), BiometricImportController (preview, upload), BusinessPartyController (create/update/deactivate/cleanup-phone), EmployeeController and CategoryController (create/update/deactivate), and the class-level OperationsController gate. No other hierarchy is inferred: `ADMIN` does not inherit `HR_MANAGER`, `HR_MANAGER` does not inherit `HR_REVIEWER`, and domain roles stay confined to their own domains. Frontend `AuthService.hasAnyRole`/`hasMenuAccess` already treat `SUPER_ADMIN` and `ADMIN` equivalently, so UI and API stay aligned. Four new `AuthSecurityIntegrationTests` cases prove it: `superAdminOnlyCanAccessEveryAdminEndpoint`, `superAdminOnlyCanCreateAndReviewAttendanceReports`, `superAdminOnlyCanUploadAndManageBiometricImports`, and `unrelatedDomainRolesRemainForbidden` (where `@Valid` runs before method security, so no-body endpoints assert the 403 proofs).
- **P2-03 — Exact-commit Release Verification Gate**: Verified against HEAD `ee8356eaec18cef21fa099d877baa96d64f60238` (`fm_bemo_consolidated`) with the working-tree changes below also under test (Testcontainers singleton fix, `LiquibaseUpgradePathTests`, `fix_*.py` removal):  - **Backend `./gradlew clean test`**: BUILD SUCCESSFUL — 209 tests, 54 suites, 0 failures / 0 errors / 0 skipped.
  - **PostgreSQL/Testcontainers concurrency**: 24/24 pass — PunchSourceIdentity 3, WorkforceImportCommit 10, SupplierPayment 10, ReportingBulkDecision 1. Fixed `PostgresIntegrationTest`: the previous `@Container static` restarted Postgres per class while Spring's cached context kept the first datasource URL, causing flaky `ConnectException`; it now starts one per-JVM singleton container kept alive for the whole test process.
  - **Fresh-database Liquibase migration**: 141 changesets apply cleanly on fresh Testcontainers Postgres with `spring.jpa.hibernate.ddl-auto=validate`.
  - **Upgrade migration from the current production schema**: new `LiquibaseUpgradePathTests` applies the released baseline `20260729_v1_v67.changelog-master.yaml` first, then the full master, and asserts the upgraded database ends with the same changeset count as a fresh install — passes.
  - **Frontend**: `npm ci` OK; `npm run check:i18n` OK (1103 keys × ar-EG/en-US); `npm test -- --watch=false` OK (17 files, 46 tests, 0 failed); `npm run build` OK (3 pre-existing SCSS budget warnings only).
  - **Production Compose**: `docker compose config` validates for both dev and prod overlays; `up --build -d` brings up db/backend/frontend with db `healthy`, backend `healthy` (Liquibase 141 changesets, `Started BemoErpApplication in ~30s`), frontend up on `:80`. Through the nginx proxy `GET /api/v1/health` returns 401 (auth enforced, backend reachable) and `GET /` returns 200.
- **P3-01 — One-Time Repair Script Removal**: Deleted 14 one-time `fix_*.py` repair scripts from the repo root (fix_api_exception_handler, fix_doc_versions, fix_fe_tests, fix_fe_tests2, fix_fe_tests3, fix_h2_tenant_features, fix_import_reversal_n_plus_one, fix_settlement_recalculation, fix_tenant_features, fix_tenant_features2, fix_toolchains, fix_workforce_import_v01, fix_workforce_import_v01_2, fix_workforce_import_v01_3). Each was inspected and its change confirmed already present in source; none were reusable operational utilities, so no `tools/` directory was created. `git status` confirms only the script deletions, and the backend suite re-ran green (209 tests) after removal.

---

- **QA Remediation — Journal Posting Idempotency & Confirmation (QA-001, UX-006 slice)**: Journal post now sends `POST /api/v1/finance/journal-entries/{id}/post` with `{operationId, expectedVersion, reason}`. `operationId` is a `crypto.randomUUID` kept per entry and reused after network timeouts (HTTP status 0) to guarantee exactly-once semantics; it is cleared only after success or a deterministic failure (409 included) and rethrown errors leave the dialog open. A shared typed `ConfirmDialogService` (`confirmAndRun`) renders title/message/details/danger/error/busy states in the shell overlay with localized copy (`common.confirm`/`common.cancel`/`common.processing` replacing hardcoded Arabic), and the journal page separates loading/error/saving/posting per-row state signals with retry affordances. Journal line totals and balance are computed client-side for validation only. Liquibase V98 loads the new bilingual keys. Verified: 5 Vitest cases green (UUID+expectedVersion payload, single active request on double-click, timeout retry reuses the operation ID, 409 clears+propagates, success clears+reloads), `npm run check:i18n` green (1127 keys × ar-EG/en-US), `npm run build` green (3 pre-existing SCSS budget warnings only).
- **QA Remediation — Menu-Access Hardening (AUTH-001)**: Backend `AuthApi.UserResponse`/`MeResponse` now expose `allowedMenus` + `menuAccessMode` (`"ALL"` for SUPER_ADMIN/ADMIN, else `"SELECTED"`); `AppUser.getAllowedMenus()` returns an empty set for null/blank (default-menu fail-open removed) and `AuthService.hasMenuAccess` fails closed for SELECTED users with empty/missing `allowedMenus`, so legacy sessions can no longer keep stale menu grants. Frontend adds `MenuAccessMode`, a `menuAccessGuard` (requires `route.data['menuId']`, denies to `/forbidden` on missing), and `menuId`+guard wiring on every protected route (`app.routes.ts`, `workforce.routes.ts`). Verified: `auth.service.spec.ts` 15/15, `auth.guard.spec.ts` 11/11, full FE 66/66 green, backend `compileJava --rerun-tasks` green. 3 pre-existing H2 `ON CONFLICT` failures (import batches/biometric import) confirmed unchanged via git-stash baseline; Docker/Testcontainers tests excluded (daemon down, per instruction).
- **QA Remediation — Workforce Role Matrix (AUTH-002)**: Replaced the blanket `WORKFORCE_ROLES` (which wrongly included `HR_MANAGER`/`HR_REVIEWER`) with backend-aligned per-route matrices derived from the workforce controllers' `@PreAuthorize`: `WORKFORCE_BASE_ROLES` (MANAGER/REVIEWER/FINANCE) for dashboard, contractors, workers, categories, labor-requests, attendance, settlement-periods, advances; `WORKFORCE_IMPORT_ROLES` (MANAGER/REVIEWER) for reports-import; `WORKFORCE_ACCOUNT_ROLES` (MANAGER/FINANCE) for contractor-accounts. Applied to `workforce.routes.ts`, `app.routes.ts` parent, and `app-shell.component.ts` nav items. Verified: full FE 66/66 green, `npm run build` green (3 pre-existing SCSS budget warnings only), `npm run check:i18n` green (1127 keys × ar-EG/en-US).
- **QA Remediation — Remove `HR_MANAGER` from Non-HR Write Authz (AUTH-003)**: Backend `@PreAuthorize` write grants for `HR_MANAGER` removed from all non-HR controllers while read access is preserved (class-level annotations unchanged): `AccountingController` (create/update account, create/post/reverse journal entry → `FINANCE_MANAGER, ACCOUNTANT`; reverse → `FINANCE_MANAGER`), `TreasuryController` (bank/tax/currency writes → `FINANCE_MANAGER, ACCOUNTANT, TREASURY_USER`), `FiscalPeriodController` (generate-year/status → `FINANCE_MANAGER`), `SalesController` (create/confirm SO → `SALES_MANAGER`), `ManufacturingController` (BOM/order/inspection writes → `MANUFACTURING_MANAGER`), `ProcurementController` (PO/GRN/invoice/payment writes → `PROCUREMENT_MANAGER`), `OperationsController` (item/transaction/advance/category/UOM writes now method-level `SUPER_ADMIN, ADMIN`; class-level `HR_MANAGER` read retained). Frontend route + nav role arrays drop `HR_MANAGER` for operations, procurement, sales, production, quality, accounts, journal-entries, banks, tax-currency, fiscal-periods (payroll/categories/employees/imports/parties keep it as HR modules). Verified: backend `compileJava --rerun-tasks` green; non-Docker suite 49 classes/195 tests with only the 3 pre-existing H2 `ON CONFLICT` failures (AuthSecurityIntegrationTests 30 tests/1 known H2 fail); FE 66/66 green.
- **QA Remediation — Employee Deactivation UX & Error Handling (EMP-001)**: Employee deactivate (previously a bare `×` button labeled `common.delete`) now uses the shared `ConfirmDialogService.confirmAndRun`: explicit "Deactivate employee" confirm with danger styling, a warning that deactivation closes the employee's current assignment, and code/category details; the success toast fires only after the API resolves, and failures keep the dialog open with an error + retry (the store no longer swallows errors — `deactivate` rethrows, matching `save`). Inactive employees get a clear "Reactivate" action that PUTs the full payload with `active: true` and reloads. Row-actions buttons were widened for labels (`danger-link`/`success-link`). Liquibase V99 loads new bilingual keys (`employees.deactivate`, `employees.deactivateTitle`, `employees.deactivateDanger`, `employees.deactivateSuccess`, `employees.reactivate`, `employees.reactivateTitle`, `employees.reactivateConfirm`, `employees.reactivateSuccess`). Verified: `employees.store.spec.ts` 4/4 green (DELETE+reload, failure propagates, reactivate PUT payload active:true + reload, failure propagates), full FE 19 files/70 tests green, `npm run build` green (3 pre-existing SCSS budget warnings only), `npm run check:i18n` green (1130 keys × ar-EG/en-US), backend `compileJava --rerun-tasks` green, non-Docker context tests 35 tests with only the 3 pre-existing H2 `ON CONFLICT` failures (V99 migration applies cleanly on H2).
- **AUTH-USER-ACCESS-001 — Add/Edit User Access Guidance (P0)**: Backend introduces the canonical `AccessCatalog` domain (19 roles, 34 pages, 5 segregation-of-duties rules, sensitive-permission set, 22 guided business needs) exposed via `GET /api/v1/access/catalog`; `POST /api/v1/access/preview` computes effective access (menu visibility + role grants + action-level precedence) and `POST /api/v1/users/access/validate` revalidates assignments authoritatively before save (super-admin delegation, self-role-change guard, block/warn conflicts, `ACCESS_UNKNOWN_ROLE`/`ACCESS_SELF_ROLE_MODIFICATION`/`ACCESS_CONFLICT_BLOCKED`). `AuthService.create/update` run validation with added/removed role-diff audit. Frontend rewrites the Add/Edit User drawer with role cards (sensitivity/kind badges, page/action counts, search), guided needs-based role suggestion with least-privilege tie-breaking and broader-role warnings, find-access-by-page search, grouped module menus, effective-access preview table, before/after edit diff box, sensitive/conflict/warning boxes, and a backend-authoritative Validate step gating save. Liquibase V100 loads 182 bilingual keys; V101 backfills 9 legacy keys that predated the DB-backed translation tables (login/settings/reports/sidebar). Full-app `check:i18n` passes (1165 keys × ar-EG/en-US); V100 CSV hardened against unquoted embedded semicolons. New `AccessCatalogServiceTests` (12 cases) and `access.service.spec.ts` (10 cases) added. **DOC-STATUS-001 (resolved)**: the catalog page count is reconciled to the real `AccessCatalog` enumeration at all times — evidence `catalog_entries = 34` from `be/src/main/java/com/bemo/hr/access/domain/AccessCatalog.java` (`pages` list, lines 285-529), kept in lockstep with the map (46 as of WP-04/V347 which added the FIXED_ASSETS finance page + P_ASSET_READ/MANAGE; 45 before it; 34 after B-1 added the BUDGETS page; was 33 after ACCESS-APPROVAL-001 added the two approval pages; was 31 before).
- **QA Remediation — Attendance Bulk Decisions & Business Parties (P0)**: Bulk decision endpoint `POST /api/v1/reports/{id}/bulk-decision` with operation-ID idempotency plus `PUT /api/v1/reports/{id}/downtime-decision`; new `AttendanceDecision` types `ABSENCE`/`OFFICIAL_HOLIDAY`/`INDIVIDUAL_REVIEW`; enhanced report filter panel (date, category, attendance condition, review status), recommendation counts from filtered data, and persisted downtime. Business Parties extended with `nameEn`, `email`, `address`, `relationshipStartDate`, `relationshipEndDate` across entity, API DTOs, frontend model/form/template with `@Email`/phone/taxID validation, managed-type restricted to `DIRECT`/`MANAGED` (responsible party required for managed), `POST /api/v1/parties/cleanup-phone`, and Liquibase V44 columns + data cleanup.
- **QA Remediation — Procurement Document Flow (P0)**: GoodsReceipt/SupplierInvoice/SupplierPayment entities + repos + API DTOs + service. PO transitions `receive()` (ISSUED→RECEIVED) and `cancel()`; GRN creation auto-transitions the PO to RECEIVED; invoice creation auto-creates the partner ledger debit, payment creation auto-marks the invoice PAID and posts the ledger credit. Controllers: `GET/POST /goods-receipts`, `/invoices`, `/payments`, `POST /orders/{id}/receive`, `POST /orders/{id}/cancel`. Liquibase V45 (`goods_receipt_lines`, GRN notes, invoice discount/tax/net/due_date/notes). Frontend tabbed procurement page (PO/GRN/Invoices/Payments) with centered dialogs, receive/cancel actions, invoice discount/tax net calc, and payment linked to unpaid invoices.
- **QA Remediation — Attendance Report Bulk Decisions & Business Parties UI (P0)**: Filter panel with date/category/attendance-condition/review-status, recommendation counts from the filtered dataset, bulk decision across filtered rows with operation-ID idempotency and audit logging, downtime decision persistence, and the extended business-party form (nameEn/email/address/relationship dates) with managed-type validation and phone cleanup.
- **QA Remediation — remaining P1 batch (EMP-002, FIN-002, PROC-001, UX-001/002/003/004/005/006, TECH-001, H2 ON CONFLICT)**: EMP-002 employees drawer dirty-check (`closeDrawer()` shows `common.unsavedTitle/Message` + danger discard via `ConfirmDialogService.confirmAndRun` when the form is dirty; `private closing` re-entrancy guard; page-level Escape handler routes through `closeDrawer()`). FIN-002 journal line validators (`lineErrors` Map per line: `journal.lineAccountRequired`, `journal.lineNegativeAmount`, `journal.lineEmptyAmount`; `submitAttempted` blocks `submitEntry()` with `journal.lineValidationError`; submit disabled while errors exist). PROC-001 invoice `discountAmount`/`taxAmount` `Validators.min(0)`, `invNetAmount()` preview now exactly `total - discount + tax` (backend parity, no clamping), `submitInvoice` blocks `discount > total` (`procurement.invoiceDiscountExceedsTotal`) and `dueDate < invoiceDate` (`procurement.invoiceDueDateBeforeInvoiceDate`). UX-001/002 date-order + conditional-required: PO/GRN numbers become `Validators.required` when automatic numbering is disabled (`applyNumberingValidators()` wired to `loadNumberingSettings`), employees save blocks `activeTo < activeFrom` (`employees.activeToBeforeActiveFrom`). UX-003 PO issue/cancel converted to `confirmAndRun` with `procurement.issuePo.*`/`procurement.cancelPo.*` keys, details labels (`poNumber`/`poSupplier`/`poTotal`), errors keep the dialog open. UX-004 server-side sequences: `resolvePoNumber`/`resolveGrnNumber`/`nextDocumentNumber`/`highestExistingNumber` verified for PO/GRN auto/manual parity; added the missing `PROC_PAYMENT_NUMBER_REQUIRED` backend guard for blank payment numbers (invoices legitimately use external supplier numbers / internal reference + reason for managed suppliers). UX-005 i18n audit: procurement page fully converted to `i18n.t('procurement.*')` (zero static Arabic/English UI labels; `'المستودع الرئيسي'` kept only as server-stored data default), `poStatusLabel` maps to `procurement.poStatus.*` keys, export column labels localized. UX-006 state separation audit: procurement page keeps separate `loading`/`submitting`/`error` signals, per-row actions use the dialog's busy/error states; journal page already separates loading/error/saving/posting. TECH-001: `cloudflared-windows-amd64.exe` removed from tracking + `.gitignore` `cloudflared-*`. H2 parity: `ImportBatchRepository` `ON CONFLICT` clauses relaxed to bare `ON CONFLICT DO NOTHING` so the full suite runs on H2 without Docker. Liquibase V103 loads the 231 new bilingual key pairs (drawer/guard/journal/procurement). Verified: backend full suite 208 tests / 0 failures (`BUILD SUCCESSFUL in 10m 25s`, `-PskipDockerTests`), FE 22 files / 114 tests green, `npm run build` green (4 pre-existing SCSS budget warnings on dashboard/app-shell/users/report-review only), `npm run check:i18n` green (1388 keys × ar-EG/en-US, no static dictionaries).

- **FIN-002/UX-004 — Unified Document Numbering (JV/PMT auto-sequences)**: Replaced the legacy `ProcurementDocumentSequence` with a unified per-tenant/year engine `com.bemo.hr.shared.numbering` (`DocumentNumberSequence`, pessimistic-lock `DocumentNumberSequenceRepository`, `DocumentNumberService.next(type, prefix, date)` → `{PREFIX}-{YEAR}-{N:05d}` with rollover). Journal entries and supplier payments now auto-generate (`JV-2026-00001`, `PMT-2026-00001`) unless the tenant disables automatic document numbering (`automatic_document_numbering` on `apps`); manual mode enforces required + unique checks (`JOURNAL_NUMBER_REQUIRED`/`JOURNAL_NUMBER_DUPLICATE`, `PROC_PAYMENT_NUMBER_REQUIRED`/`PROC_PAYMENT_NUMBER_EXISTS`). New endpoints `GET /api/v1/finance/numbering-settings`; journal and procurement pages expose a `NumberingSettings` toggle and switch validators/auto-prefix hints per mode; settings page carries the document-numbering group. Liquibase V116 (`document_number_sequences`, `apps.automatic_document_numbering`) + H2 mirror (with `"year"` quoted for H2). New suites: `DocumentNumberServiceTests` (4), `JournalEntryServiceTests` (4), `SupplierPaymentValidationTests` (3) — all green.
- **ACCESS-APPROVAL-001 — Approval Screens in the Access Catalog (P0)**: The Pending Approvals and Workflow Definitions screens were missing from `AccessCatalog`, so user saves selecting `approvals-my-tasks`/`approvals-workflows` were rejected with `ACCESS_UNKNOWN_MENU`. Added `PENDING_APPROVALS` (`/approvals/my-tasks`, `approvals-my-tasks`) and `WORKFLOW_DEFINITIONS` (`/approvals/definitions`, `approvals-workflows`, ADMIN/SUPER_ADMIN route) pages, new permission codes `approvals.read`/`approvals.decide`/`workflowDefinitions.read`/`workflowDefinitions.manage`, grants aligned with the `ApprovalController` `@PreAuthorize` (WORKFORCE_MANAGER, WORKFORCE_REVIEWER, FINANCE_MANAGER, ACCOUNTANT, PROCUREMENT_MANAGER, PROCUREMENT_USER, HR_MANAGER + ADMIN/SUPER_ADMIN), `DECIDE` added to the effective-access level precedence, and a `MANAGE_APPROVALS` guided need. Liquibase V117 appends both menu IDs to existing `app_users.allowed_menus` (idempotent guards) + bilingual `access.pages.needs.manage-approvals` rows; `AuthService.ensureBootstrapAppAdmin` default menus now include both IDs. Frontend already registered the menus in `app-shell` nav, `users.page` `menuOptions`/`workspace.approvals`, routes, and `hasMenuAccess`/`visible()` admin bypass — verified present. `AccessCatalogServiceTests` extended to 34 cases (catalog exposure, menu validation, admin REVIEW, WORKFORCE_MANAGER DECIDE, admin-only route guard).
- **A-4 PHASE1-REPORTS-001 — Complete Phase-1 report surfaces (P1)**: Inventoried the live report surfaces (`ReportController` `/api/v1/reports` list/details/preview/decisions/export; `DashboardController` `/api/v1/dashboard` + summary/attendance-chart/payroll-summary/department-metrics; `DataExportController` `/api/v1/exports/{scope}.xlsx`) and closed the two remaining gaps. **Multi-period trends API**: `DashboardApi.TrendPoint` + `DashboardService.trends(months)` (capped 1..24, oldest-first; per-month scheduled/present/attendance-rate/exception-days/overtime + payroll paid/pending counts and gross/paid totals) + `GET /api/v1/dashboard/trends?months=N`. **Filterable trends export**: `DataExportService.trends(months, options)` + `GET /api/v1/exports/trends.xlsx?months=N` with Arabic filename. **Frontend**: `TrendPoint` model, `DashboardStore.loadTrends`/`downloadTrends`, multi-period trends table on the dashboard with month-count selector (3/6/12/24) and Excel export button. Liquibase **V121** (`20260807_v121_trends_export_translations.{yaml,csv}`, 44 rows: export sheet/columns + 12 dashboard UI keys × ar-EG/en-US) registered in `next` + `test-h2`. Evidence: `DashboardServiceTrendsTests` (2), `dashboard.store.spec.ts` (3); backend 252/55/0, frontend 139/27/0, `check:i18n` 1603 keys, `check:hardcoded` 0/35, `ng build` green, H2 context green, error-codes gate PASS.
- **E-1 I18N-RETRY-001 — Hardcoded-UI i18n elimination (P0)**: New scanner `fe/tools/check-hardcoded-strings.mjs` flags bare text nodes + display literals across all 35 Angular templates (handles `i18n.t('k') || 'fallback'` chains, boundary-punctuation strip, EGP unit `ج.م` allowlist); wired as `npm run check:hardcoded` and into the `frontend` CI job. All templates fully i18n-ized (accounts, tax-currency, quality, parties, payroll, operations, settings, procurement, employees, users, reports, report-review, skeleton). Data bug fixed: report-review hardcoded `08:00 AM` expected-in replaced with `—`. Liquibase V119 (`20260807_v119_hardcoded_ui_translations.{yaml,csv}`, 167 keys × ar-EG/en-US, ids `v119-001`…`v119-334`, generator `/tmp/opencode/gen-v119.cjs` auto-filters 18 `operations.*` keys already in V52 to avoid `UQ_TRANSLATIONS_KEY_LOCALE` dupes) registered in `next` + `test-h2`. Evidence: scanner zero findings (35 templates), `check:i18n` 1591 keys, `ng test` 136/136, `ng build` green, H2 context green (`MeIdentityIntegrationTests` + `AccessCatalogServiceTests` + `JournalEntryServiceTests`).
- **FINAL-001/FINAL-002 — Release-gate fixes (Audit Logs load error + attendance decision retry)**: **FINAL-002 root cause was frontend-only**: on API failure the audit-logs page rendered the error banner *and* the table's `@empty` block simultaneously (false "لا توجد سجلات تدقيق مسجلة بعد." + bogus `0–0 / 0` pagination). `audit-logs.page.ts` gained `retry()`; `audit-logs.page.html` now renders a dedicated `.load-error` block (`audit.loadErrorTitle` + `audit.loadErrorHint` + backend detail + `common.retry` button) via `@if (error())` and only renders the table + pagination in the `@else` branch (stale duplicate banner removed); new `.load-error` SCSS. **FINAL-001 backend verified correct** (`ReportingService.decideDaily` persists in a single transaction; `@Version` optimistic locking via `expectedVersion`); the frontend gap closed by making the decision prompt's confirm button switch from `common.confirm` to `common.retry` when a persisted failure error is shown — the modal stays open with the input preserved, the button re-enables on completion, and no optimistic counter changes on failure. Liquibase **V149** `20260809_v149_audit_log_error_state_translations.{yaml,csv}` (2 keys × ar-EG/en-US, ids `v149-001/002` per locale) registered in `next` + `test-h2`. Evidence: backend **297/62/0**, frontend **211 tests / 33 files / 0 failures** (+3: audit-logs error-state DOM + retry-refire, report-review retry-labeled button), `check:i18n` **1798 keys**, `check:hardcoded` 0/37+112, `ng build` green, H2 context green (`MeIdentityIntegrationTests`), error-codes **262/262**.
- **E-2 — Global exception handler alignment + error-code parity (P0)**: `ApiExceptionHandler` is the single uniform `@RestControllerAdvice`; frontend holds no static error map (`api-error.ts` resolves `apiError.code` through the same DB translation tables). Audit found **26 backend exception codes shipped without translation rows** (approval engine, biometric sources, shortcuts, procurement, notifications, fiscal periods, journal numbering) — fixed by Liquibase **V120** (`20260807_v120_exception_i18n_fix.{yaml,csv}`, 26 keys × ar-EG/en-US) registered in `next` + `test-h2`, H2 context verified green. New CI gate `be/tools/check-error-codes.py` fails the build if any future code lacks a translation row (241/241 pass).
- **Frankfurter online exchange-rate hints (hint-only) — applied package**: Applied the Bortqala `BORTQALA_FRANKFURTER_INTEGRATION_2026-08-09` overlay. Backend `com.bemo.hr.finance`: `ExchangeRateHintApi/Service/Scheduler/Setting`, `ExchangeRateHintSettingRepository`, `FrankfurterExchangeRateClient` (`/v2/currencies` + `/v2/rates`), tenant-scoped `exchange_rate_hint_settings` (`@TenantId` `app_id`, `@Version`, enabled default, 1–168h refresh interval), `Currency` gains `reference_*` columns + `updateReferenceRate`/`markReferenceUnavailable` (never overwrites `Currency.exchangeRate`), and `TreasuryController` gains `GET/PUT /api/v1/finance/exchange-rate-hints/settings` + `POST /exchange-rate-hints/refresh` while `GET /currencies` returns the reference metadata. Scheduler scans every 5 min for due tenants. Frontend `tax-currency.page.{ts,html,scss}`: online-reference panel (configured vs reference rate, provider date, last/next sync, difference %, admin interval config + manual refresh). **Liquibase renumbered V148/V149 → V150/V151** (the package shipped colliding IDs): **V150** schema (`exchange_rate_hint_settings` + unique app FK + 6 `currencies` reference columns), **V151** translations (32 keys × ar-EG/en-US = 64 rows `v151-001`…`v151-064`), registered in `next` + `test-h2`. Fixed 3 package defects (duplicate CSV ids `v149-057/058` for `FRANKFURTER_UNAVAILABLE`/`onlineHoursUnit`, unquoted `;` in `onlineEnabledHint`, unquoted `:` in the V150 `comment:`) and added 2 keys the TS used but the package omitted (`taxCurrency.taxCreated`/`currencyCreated`, rows 061–064). Evidence: backend **299 tests / 63 suites / 0 failures** (+2 `ExchangeRateHintServiceTest`), frontend **211 tests / 33 files / 0 failures**, `check:i18n` **1829 keys**, `check:hardcoded` 0/37+112, `ng build` green, H2 context green, error-codes **262/262**. Baselines refreshed (`be/tools/check-test-count.py` → 299/63).- **E-3 — Test-count evidence + CI regression gate (P0)**: `docs/TEST_EVIDENCE.md` records baselines with date + SHA (backend **250 tests / 54 suites / 0 failures** non-Docker, verified 2026-08-07 at HEAD `498227e`; frontend **136 tests / 26 files** under node v24.18.1; earlier entries for 290-total/259-green and `ee8356e` 209). CI gates: `be/tools/check-test-count.py` (after `./gradlew clean test check`) and `fe/tools/check-test-count.mjs` (ANSI-stripped parser fed by the `ng test --watch=false` stream) — both fail the build on regression below baseline.
- **E-4 — Build tooling (P0)**: `docs/BUILD_TOOLING.md` documents the native-Gradle-on-`/mnt/d` flakiness and the `/tmp/opencode/be-build` rsync mirror workaround; `fe/.nvmrc` pins Node 24; `fe/package.json` `engines` is `">=22.0.0 <25"`; the `frontend` CI job enforces it with a `Node version guard` step before `npm ci`.
- **RELEASE-HEAD-001 root cause (A-2)**: Identified 2026-08-07 via the public GitHub REST API — all 13 branch workflow runs (17→30, 2026-08-03→08-06) fail with every job concluding `failure` 2–4 s after start with `steps: 0` (no checkout/compile/test ever runs), i.e. the GitHub Actions account/billing runner lock, **not** a code failure. No commit on the branch can go green until repo owner `Mohamed-Hammada` resolves the account billing/runner availability. Evidence: `actions/runs` + `attempts/1/jobs`; roadmap A-2 + `PROJECT_MAP.md` blocker entry updated. CI re-run step remains blocked on the owner.
- **B-1 — Budget Control & Encumbrance (Phase 1 business, P0)**: Connected budget control into the procurement flow. Backend adds `com.bemo.hr.budget` (entities `Budget`/`Encumbrance`, repos, `BudgetService` with availability checks, `BudgetPeriodType`/`EncumbranceStatus`) and `BudgetController` (`/api/v1/budget`: `/budgets` list/create/update, `/status` availability, `/encumbrances`, `/export.xlsx`). Purchase orders carry an optional `departmentId` (`assignDepartment`); issuing a PO with a department **encumbers** against that budget, GRN receipt/liquidation and PO cancellation **release** the encumbrance, and supplier invoices **liquidate** against commitments — with `budget.blocking` deciding warning vs hard block and over-budget rules (`BUDGET_*` exception keys, V123). Access catalog gains the `BUDGETS` page (`/finance/budgets`, `budgets` menu id, `P_BUDGET_READ`/`P_BUDGET_MANAGE`) — catalog now 34 pages. Liquibase **V122** schema (`budgets`, `budget_encumbrances`), **V123** translations (7 budget exception codes + UI + `procurement.department`), **V124** `allowed_menus` append, **V125** `nav.budgetsHint`/`procurement.departmentHint`. Frontend: new `finance/budgets` feature (models, service, budgets page with budgets/status/encumbrances tabs, MONTHLY prefill via reactive `valueChanges`, Excel export), menu registration (routes, `app-shell` nav, `auth.service` gate, `access-catalog-contract`, `users.page` options), and procurement page PO form gains the department selector + column. Evidence: backend **268 tests / 56 suites / 0 failures**, frontend **150 tests / 28 files / 0 failures**, `check:i18n` 1645 keys, `check:hardcoded` 0/36, `ng build` green. Fixed en route: V125 CSV stray `;` broke the H2 Liquibase context (53 context-load failures, one root cause); `be/tools/check-test-count.py` BASE_DIR resolved to `be/` not `be/tools/`; `budgets.page` prefill switched from a DOM `(change)` handler to a reactive subscription.

- **ADV-P0.1 — Inventory Valuation + Inventory GL (DONE 2026-08-09)**: Liquibase V154 adds one tenant policy (FIFO/weighted average, backdating and GL controls), optimistic FIFO cost layers, immutable per-movement cost evidence, and idempotent revaluations. `InventoryValuationService` takes a pessimistic item lock, blocks negative issues/backdating/closed periods, explains every cost, and posts balanced inventory/receipt-offset/COGS/variance journals. Procurement GRNs pass the PO line unit price. Operations APIs expose valuation settings/report/movement drill-down/revaluation; the Angular workspace adds method/account settings, item valuation, opening-gap visibility, movement explanations, revaluation, and two localized Excel sheets. V155 provides bilingual UI plus all new error codes. Evidence: backend **312 tests / 65 suites / 0 failures**, frontend **223 tests / 35 files / 0 failures**, `check:i18n` **1875**, `check:hardcoded` **0/38 HTML + 113 TS**, error-code gate **267/267**, production build green.
- **ADV-P0.2 — Supplier Onboarding + Supplier 360 (DONE 2026-08-09)**: Liquibase V156 adds supplier lifecycle/classification/bank-verification fields plus tenant-scoped compliance-document and bank-account evidence; V157 supplies bilingual UI and stable exception messages. Supplier creation now starts at `REQUESTED`, supports optional shared approval, and must pass verified/current mandatory documents before approval and a verified primary bank before activation/payment. Duplicate tax IDs and normalized IBANs are blocked; real allowlisted files (≤5 MB) are stored/downloaded securely; suspended/blacklisted suppliers cannot enter procurement. Angular adds duplicate warnings, compliance and bank workbenches, lifecycle actions, and Supplier 360. Evidence: backend **320/66**, frontend **227/36**, H2 migration green, i18n **1908**, hardcoded 0, error parity **271/271**, production build green.
- **ADV-P0.3 — Bank Reconciliation + Cash Position (DONE 2026-08-09)**: Liquibase V158 adds statement, versioned-line, and reversible match evidence plus bank-account currency; V159 provides bilingual workbench/error copy. Real CSV files are parsed with quoted-field support, hash/references prevent replay, and opening + movements must equal closing. Posted bank-GL journals drive ranked suggestions; exact, partial, manual, and fee matches are supported. Fee matches create balanced posted journals; reversal reopens the line and reverses generated fee journals; closed periods block writes and statement aggregates are pessimistically locked. The banks page adds import/reconciliation/cash tabs and never mixes currency totals. Evidence: backend **327/67**, frontend **231/37**, H2/i18n/hardcoded/build gates green.
- **ADV-P0.4 — Advanced Approvals (DONE 2026-08-09)**: V160 snapshots definition version, document summary and effective steps, and backfills existing decisions safely on PostgreSQL/H2. Dated document-scoped delegations record acting/original users; administrator reassignment is audited; pessimistic instance locks serialize distinct ANY_N signatures; self-approval policy remains immutable. Per-step SLA drives tenant-aware one-shot escalation. The workspace adds approval/delegation/overdue summaries, aging filters, signature progress, delegated history and delegation settings. V161 provides bilingual UI/errors. Evidence: backend **332/67**, frontend **236/38**, H2/error/i18n/hardcoded/build gates green.
- **ADV-P0.5 — Advanced Attendance (DONE 2026-08-09)**: V162 adds tenant-scoped effective-dated policies and immutable exception evidence; resolution chooses employee over category over tenant policy, scores missing/single/late/early/excess-shift cases, and snapshots the applied version. Report generation assigns valid cross-midnight punches to the previous work date, aggregates exceptions idempotently, blocks approval on critical open evidence, and gates payroll by approved report plus employee/all-report blockers. V163 provides bilingual UI/errors. The report-review workbench exposes summary/filter/explanation/policy evidence and idempotent preview-before-apply bulk resolution. Evidence: backend **338/68**, frontend **238/38**, H2 **284/284** error/i18n/hardcoded/build gates green.
- **ADV-EPIC1-AR — Accounts Receivable Foundation (DONE 2026-08-09)**: V164 adds customer credit profiles, versioned invoices, idempotent receipts, locked allocations and collection tasks; V165 supplies bilingual UI/errors. Sales-order confirmation and invoice issue enforce holds/available credit. Issued invoices and receipts create customer partner-ledger evidence; receipts support advances and partial/full allocation without exceeding either source or outstanding. Backend aging drives the integrated invoice/receipt/collection/credit workbench. Evidence: backend **343/69**, frontend **241/39**, H2, **300/300** errors, i18n/hardcoded/production build green.
- **ADV-EPIC2-ENTITLEMENTS — Canonical Module Entitlements (DONE 2026-08-09)**: `EntitlementCatalog` owns all feature defaults, module grouping, dependencies and API prefixes; `TenantFeatureService`, identity and `TenantFeatureInterceptor` consume that single source. V166 adds mandatory reason to versioned tenant overrides. Super Admin API/UI shows effective state and blocks missing dependencies or active dependents; downgrade preserves data and every mutation audits before/after/reason. V167 provides bilingual UI/errors. Evidence: backend **347/70**, frontend **244/40**, H2, **302/302** errors, i18n/hardcoded/production build green.
- **ADV-EPIC3-INDUSTRY-PACK — Contractor Workforce Egypt Pack (DONE 2026-08-10)**: V168 defines global versioned packs, tenant install/customization evidence and prerequisite-aware onboarding; V169 seeds `CONTRACTOR_WORKFORCE_EG`. Operation-idempotent install activates required Epic-2 entitlements and snapshots terminology, workforce dashboard, four KPIs, three roles and import templates. Seven go-live steps include an optional advances step; upgrade replay is safe and never overwrites customized settings. Super Admin UI covers install/progress/settings/upgrade. Evidence: backend **352/71**, frontend **247/41**, H2, **308/308** errors, i18n/hardcoded/production build green.
- **ADV-EPIC4-TRIAL-DEMO — Trial Lifecycle + Safe Demo Template (DONE 2026-08-10)**: V170 stores trial dates/commercial state on the existing tenant, versioned demo templates, and isolated template-owned sample rows; V171 supplies bilingual UI/errors. A central policy makes expired trials commercially read-only while keeping reads available. Start, conversion and reset are operation-idempotent and audited; conversion preserves tenant identity/business records, and reset is rejected for non-demo tenants and cannot delete ERP aggregates. Super Admin settings expose lifecycle, access, template, samples and reset. Evidence: backend **361/73**, frontend **250/42**, H2, **314/314** errors, i18n/hardcoded/production build green.
- **ADV-EPIC5-ONBOARDING — Guided Onboarding + Data Quality (DONE 2026-08-10)**: V172 persists immutable assessment snapshots and serializes commands under the installed pack; V173 provides bilingual UI/errors. The service reuses the selected pack's steps/dependencies, auto-completes only from tenant evidence (successful `IMPORTED` included), preserves optional `SKIPPED`, calculates setup/quality, and requires all mandatory steps plus ≥80 quality for `READY`. Admin UI exposes blockers and direct action routes. Evidence: backend **365/74**, frontend **253/43**, H2, **316/316** errors, i18n/hardcoded/build green.
- **ADV-EPIC6-ACTION-CENTER — Smart Action Center 2.0 (DONE 2026-08-10)**: V174 enriches tenant/recipient-owned notifications with exception, localized impact/reason/recommendation, amount/currency, safe internal action, role targets and queue index; V175 supplies bilingual card copy. Backend ranks severity + unread + decisive role match and retains old payload compatibility, ownership and audit. The navbar panel renders the full action card and routes directly. Evidence: backend **368/74**, frontend **255/44**, H2, **316/316** errors, i18n/hardcoded/build green.
- **ADV-EPIC7-PARTNER-RISK — Supplier/Contractor Scores + Risk Rules (DONE 2026-08-10)**: V176 adds tenant/versioned band thresholds and immutable retry-safe score snapshots; V177 provides bilingual UI/errors. Deterministic formulas reuse supplier lifecycle/compliance/bank/profile and contractor lifecycle/worker/settlement/issue evidence. Every weighted component exposes status and fix route; refresh is tenant-locked/audited and only admins edit thresholds. Evidence: backend **372/75**, frontend **258/45**, H2, **318/318** errors, i18n/hardcoded/build green.
- **ADV-EPIC9-PRODUCT-ANALYTICS — Privacy-Limited Analytics + Activation (DONE 2026-08-10)**: V178 provides tenant/idempotent events, durable daily aggregates and six one-shot milestones; V179 supplies bilingual UI/errors. Strict scalar allowlist plus route query stripping blocks sensitive generic payloads. A proxy-separated safe sink cannot roll back ERP work; raw retention preserves aggregates/milestones; tenant summaries are isolated and platform summary is Super Admin-only. Evidence: backend **377/76**, frontend **261/47**, H2, **320/320** errors, i18n/hardcoded/build green.
- **ADV-EPIC10-SUBSCRIPTION — Subscription + Plan Control Plane (DONE 2026-08-10)**: V180 provides data-driven plan definitions, tenant/versioned lifecycle and immutable operation-idempotent change history; V181 supplies bilingual UI/errors. Plan changes lock the tenant, synchronize canonical entitlements in dependency-safe order, preserve ERP data on downgrade, disable commercial features on cancellation, audit every mutation and enforce user limits inside real account creation. Super Admin settings cover definitions, lifecycle, limits/usage and history. Evidence: backend **385/77**, frontend **264/48**, H2, **326/326** errors, i18n/hardcoded/build green.
- **ADV-EPIC11-SUPPORT-HEALTH — Support + Customer Health (DONE 2026-08-10)**: V182 provides tenant/idempotent tickets, deterministic priority SLA, versioned state transitions, immutable updates, privacy-limited feedback and health snapshots; V183 supplies bilingual UI/errors. Health deterministically combines seven explained dimensions totaling 100 and gives a direct action for each. A global Help entry serves all users; Admin gets queue transitions and health recalculation. Evidence: backend **390/78**, frontend **268/49**, H2, **332/332** errors, i18n/hardcoded/build green.
- **ADV-EPIC12-FOOD-DISTRIBUTION — Second Vertical Pack (DONE 2026-08-10)**: V184 makes roles/KPIs/templates/defaults/onboarding metadata-owned per pack and seeds `FOOD_DISTRIBUTION_EG` with dependency-closed modules, FEFO/credit/expiry policy, four roles, nine distribution KPIs, four imports and nine go-live steps; V185 localizes it. Existing non-destructive customization/upgrade behavior remains intact and the generic UI renders both packs. Evidence: backend **391/78**, frontend **269/49**, H2, **332/332** errors, i18n/hardcoded/build green.
- **EPIC-PROC-01 — Single Goods Receipt Receiving Path (DONE 2026-08-10)**: Deprecated direct status-only receive endpoint `POST /orders/{id}/receive` with `PROC_DIRECT_RECEIVE_DEPRECATED`. Enforced Goods Receipt (`POST /api/v1/trade/procurement/goods-receipts`) as the single physical receiving command that updates PO status, posts inventory stock movements, and releases budget encumbrances.
- **EPIC-PROC-02 — Supplier Return & Cancellation Protection (DONE 2026-08-10)**: Blocked PO cancellation when accepted receipts (`PO_HAS_RECEIPTS_CANNOT_CANCEL`) or linked invoices (`PO_HAS_INVOICES_CANNOT_CANCEL`) exist. Built `SupplierReturn` and `SupplierReturnLine` entities, repository, DTOs, service, REST endpoints (`GET/POST /api/v1/trade/procurement/returns`), stock movement helper (`SUPPLIER_RETURN`), Liquibase V186 schema + V187 bilingual translations, and Angular workbench tab.
- **EPIC-MFG-01 — Transactional Manufacturing Engine (DONE 2026-08-10)**: Upgraded BOM with real component lines (`BomLine`), yield quantities, waste percentages, item FKs, and versioning. Built full work order lifecycle: material readiness checks (`/orders/{id}/readiness`), starting work orders with raw material issues (`PRODUCTION_ISSUE`), completing work orders with actual output/scrap reporting, finished goods receipts (`PRODUCTION_RECEIPT`), actual unit costing, and Liquibase V188 schema + V189 bilingual translations.
- **EPIC-SUB-01 — Atomic Trial Conversion (DONE 2026-08-10)**: Whitelisted subscription management endpoints in `TrialWriteInterceptor.java` to prevent read-only trial blocks. Unified commercial state conversion (`PAID`), subscription status (`ACTIVE`), tenant lock, and operation ID idempotency in `SubscriptionService.change(...)`.
- **WP-08 Peak Clock-In Analytics (DONE 2026-08-23)**: `DashboardService.clockInHistogram(months, categoryId)` aggregates first-punch hour-of-day per attendance category over the capped 1–24-month window using the same monthly-report source as trends, bucketed in `hr.company-zone` (never hardcoded); new `GET /api/v1/dashboard/clock-in-histogram` (`DashboardApi.ClockInBucket(hour, countsByCategory)`); dashboard gains a always-visible "Peak clock-in hours" card (months selector 3/6/12, CSS bars 04–13h, per-category counts resolved via `CategoryMetric` names, graceful empty/failure states). Liquibase **V342** `20260823_v342_peak_clock_translations.{yaml,csv}` (4 keys × ar/en) in `next`+`test-h2`. Evidence: BE `DashboardServicePeakClockInTests` 3/3 (zone-correct cross-month bucketing, category filter, cap/empty handling) + TrendsTests 2/2 regression; FE **453/453** incl. new store spec (flush + failure-clears path); `check:i18n` **4546 keys**; hardcoded 0 violations (110 HTML/225 TS); production build green; H2 context with V341+V342 clean. **Re-verification corrections (no-code closures):** WP-01 core partial payments ALREADY shipped (`SupplierInvoiceResponse.paidAmount/outstandingAmount`, `PARTIALLY_PAID`, `PROC_PAYMENT_EXCEEDS_BALANCE`) — only optional installment-plan table remains; WP-06 generated-periods ALREADY shipped (`availablePeriods`/`addPeriod` overlap filter); WP-19 single-punch rule ALREADY shipped (category checkbox + ONE_PUNCH bulk accept).

- **SHELL-WP13/WP09 — Shortcut×Dialog Hardening + First-Login Push Prompt (DONE 2026-08-23)**: Root cause of "dialogs not working with shortcuts" fixed — page-level modals now register in a new `DialogStateService` (`core/shell/dialog-state.service.ts`, idempotent open/close registration inside `ModalDialogComponent.activate/deactivate`) and the shell's global keyboard handler was rewritten around a pure decision function (`shortcut-guard.util.ts`) so `/`, `?`, `Ctrl+K` and `G→key` chords are fully suppressed while ANY `app-modal-dialog` is open regardless of focus position (BUG-1/2); Escape precedence is topmost-exclusive (BUG-5); a pending G-chord is cleared when any dialog opens or the window blurs (BUG-4); `submitFormOnEnter` honors a `data-no-autosubmit` opt-out (BUG-6). Focus-trap extracted to shared `shared/ui/focus-trap.util.ts` (jsdom-safe visibility fallback layered over `getClientRects`) and applied to all four raw shell overlays (logout, action-center, quick-nav, help) which also gained `role="dialog"`/`aria-modal` where missing plus trigger capture/restore on open/close (BUG-3/8); audit found BUG-7 (duplicate shortcut toast) was ALREADY shipped upstream — scope corrected 8→7. **WP-09**: `PushPermissionPromptComponent` asks once per user×browser ~2s after login ("Enable notifications on this device?") gating on supported/configured/!subscribed/!denied, persists answer in `bemo_push_prompt_v1` ('enabled'|'later' 14-day snooze|'never'), registers as a dialog while visible, calls `WebPushService.enable(storedPreferences())` (method made public); denied users get an inline Settings hint instead of the dialog. Liquibase **V341** `20260823_v341_push_prompt_translations.{yaml,csv}` (6 keys × ar-EG/en-US) registered in `next` + `test-h2`. Evidence: frontend **452 tests / 96 files / 0 failures** (+23 across 5 new spec files: guard matrix, dialog-state, modal×state integration incl. double-registration regression caught & fixed, focus-trap, prompt gates/snooze/never-per-user), `check:i18n` **4543 keys**, `check:hardcoded` 0 violations (110 HTML + 225 TS), production build green, H2 context load with V341 clean (`MeIdentityIntegrationTests` 1/0).

- **AUDIT-F002/F003/F004 — Market-Readiness Audit Remediation (DONE 2026-08-23)**: `SalaryPayment.markAsPaid` is POSTED-only (`PAYROLL_PAYMENT_STATE_INVALID`; REVERSED is terminal for every lifecycle command). Maker/checker SoD enforced by default: `payroll_run_headers.calculated_by/approved_by` (V339) track preparer vs approver; `transitionStatus` rejects self-approval (`PAYROLL_SOD_SELF_APPROVAL`) and `recordPayment` rejects the approver as disburser (`PAYROLL_SOD_DISBURSEMENT_CONFLICT`), with `hr.payroll.sod-enabled=false` opt-out and SUPER_ADMIN break-glass bypass; V340 bilingual error translations. Cash Flow rewritten as a direct-method engine over posted GL evidence: counter-account classification per entry (financing = equity + long-term loans prefix 22/loan naming, investing = fixed assets prefix 12, operating = rest), accrual-only entries excluded, exact opening+O+I+F=closing reconciliation flag, equal-length comparative period, and a type gate on `isCashOrBank` so liability/expense accounts named "bank" can no longer pollute the cash pool; stale `FIN_CASH_FLOW_NOT_IMPLEMENTED` README claims replaced with engine documentation; FE `CashFlowReport` model extended additively (`reconciled`, `comparative`). Evidence: backend 739 tests / 189 suites / 0 failures (`-PskipDockerTests`, H2 mirror incl. V339/V340); frontend 429/91 green; i18n 4538 keys; hardcoded 0 violations; production build green; error-codes 575/575.

- **WP-07 Loans Deduction-Policy Switcher (DONE 2026-08-24)**: Employee advance collection is now explicitly configurable instead of implied by document type. Reuses the versioned `workforce_advance_policies` store (V58) — no new table; the entity canonicalizes synonyms (`AUTO_IN_PAYROLL`→`AUTO`, `MANUAL_BUTTON`→`MANUAL`) and `savePolicy` validates mode/cadence (`ADVANCE_POLICY_INVALID`), rejects overlapping later open versions (`ADVANCE_POLICY_EXISTS`). New resolver `WorkforceAdvanceService.resolveDeductionPolicy(employeeId, categoryId, date)` with EMPLOYEE → EMPLOYEE_CATEGORY → GLOBAL → defaults(AUTO+MONTHLY) precedence exposed at `GET /api/v1/workforce/advances/resolved-policy`; `isManualDeductionPolicy` gates the payroll sheet so MANUAL categories/employees get zero automatic collection (evidence recorded as an `ADVANCE_DEDUCTION` explanation row with skip-reason JSON when explanations are derived). Idempotent manual collection per (employee, period) via `POST /api/v1/workforce/advances/apply-deduction` (ledger-notes suffix replay detection, overdue-installment math capped by remaining balance, `ADVANCE_MANUAL_NOT_DUE`/`ADVANCE_NOTHING_DUE`). Frontend: Settings business tab gains an advances-policy card (global default + category exception drafts with Save-All/dirty/cancel semantics) and the Employees page gains a toolbar action visible only when a MANUAL policy resolves, confirming with the affected-employee count then applying per employee. Liquibase **V350** `20260826_v350_advance_deduction_policy_translations.{yaml,csv}` (23 keys × ar/en incl. 4 error codes) registered in `next` + `test-h2`. Evidence: backend **816 tests / 196 suites / 0 failures** (`-PskipDockerTests`, H2 mirror incl. V350; +9 workforce resolver/manual-apply/validation cases, +2 payroll gate cases); frontend **487 tests / 101 files / 0 failures** (+5 settings-policy spec cases, +2 employees gating/apply cases); error-codes gate **601/601**; translation catalog **14,026 rows PASS**; `check:i18n` **4,676 keys**; `check:hardcoded` 0 violations (114 HTML + 238 TS); production build green.
- **WP-10 Vertical-Aware User Creation & Role Templates (DONE 2026-08-25)**: The Add-User flow is now vertical-aware end-to-end. Backend `com.bemo.hr.access` gains the global role-template catalog: `UserRoleTemplate` entity (`user_role_templates`, **no** `@TenantId` — rows with `app_id IS NULL` are shared/global, tenant-owned rows shadow by code), `findForTenant` JPQL (requested ∪ inferred ∪ GENERAL verticals, ordered by sort_order/code). `UserRoleTemplateService.menuOptions()` derives the authoritative menu catalog from `AccessCatalog.pages()`, gates each on `TenantFeatureService.isEnabled`, and tags verticals by inverting FEATURE_VERTICALS; `roleTemplates(vertical)` dedupes merged templates by code (later shadow earlier), resolves tenant policy-group permission keys via grants→permissions, and suggests the top-3 groups scored by permission-prefix overlap. Endpoints on `AuthController`: `GET /api/v1/auth/users/menu-options` + `GET /api/v1/auth/users/role-templates?vertical=` (`users.read`). Liquibase **V351**: schema/create table + indexes; seed 24 templates across MEDICAL(7)/MANUFACTURING(5)/RETAIL(4)/CIVIL(3)/SERVICES(2)/GENERAL(3) via loadData CSV (`sqlCheck` idempotence, `app_id=''`→NULL normalization in changeset SQL — note: use plain `- sql:` change type; `sqlUpdate` is not a valid YAML change type); translations 29 keys × ar/en (template names + UI copy incl. `users.menuNotEnabledForVertical`) registered in `next` + `test-h2`. Frontend: `MenuOption`/`RoleTemplate` models; `AccessService.loadMenuOptions/loadRoleTemplates`; Users page converts static `USER_MENU_OPTIONS` into a computed enriched from the server catalog (static fallback preserved wholesale when endpoint empty/unavailable — AC-3), adds the job-template dropdown to the Add drawer (hidden while editing; applying merges template menus union-style, flips custom-access on, auto-selects suggested groups; still fully editable), and feature-locked menus render `.feature-locked` grayed/disabled with the vertical tooltip (AC-1); menu-registration protocol honored. Test gotcha: `ModalDialogComponent.teleportToBody()` moves opened dialogs under `document.body` — DOM assertions must query there, not `fixture.nativeElement`. Evidence: backend **822 tests / 197 suites / 0 failures** (`-PskipDockerTests`; +6 `UserRoleTemplateServiceTests`; H2 mirror incl. V351); frontend **490 tests / 101 files / 0 failures** (+3 WP-10 cases); error-codes gate **601/601**; translation catalog **14,084 rows PASS**; `check:i18n` **4,679 keys**; `check:hardcoded` 0 violations (114 HTML + 238 TS); production build green.
 - **WP-06 Generated-Report-Periods Registry (DONE 2026-08-25)**: Months that already carry a finalized attendance report can no longer be regenerated. Backend adds the derived (no new table) endpoint `GET /api/v1/reports/generated-periods?year=` (`ReportingService.generatedPeriods`, `reports.read`): `findByPeriodStartBetweenAndStatusIn` over APPROVED+EXPORTED only — DRAFT/IN_REVIEW never lock a month — returning `{from,to,type,reportId}` with epoch-millis dates via the shared Jackson customizer; missing/null/out-of-range year returns an empty list and never errors. Frontend reports page: the preset picker fetches the registry independently of page load (`ReportsStore.generated/generatedLoading` with a stale-response token so year switches discard in-flight results), finalized periods render their chip disabled/dashed with tooltip + inline note `reports.alreadyGenerated` and a `reports.viewExisting` link routerLinking to the exact existing report; all other presets stay enabled. Liquibase **V352** translations (`reports.alreadyGenerated`, `reports.viewExisting` × ar/en) registered in `next` + `test-h2`. Evidence: backend **824 tests / 197 suites / 0 failures** (+2 `ReportingServicePeriodTests` cases); frontend **493 tests / 101 files / 0 failures** (+3 WP-06 spec cases incl. DOM assertions); error-codes **601/601**; translation catalog **14,088 rows PASS**; `check:i18n` **4,681 keys**; `check:hardcoded` 0 violations; builds green.
- **WP-08 Peak Clock-In Analytics — Completion Pass (DONE 2026-08-25)**: Closed the remaining ACs on top of the V342 overlay. **Export**: `DataExportService.clockInHistogram(months, categoryId, options)` (flat hour/category/punches sheet) + `GET /api/v1/exports/clock-in-histogram.xlsx?months=&categoryId=` with localized Arabic filename; **FE analytics card**: category filter select (`changePeakCategory` refetches with categoryId), months selector keeps the active filter, legend (`peakLegend` computed, deduped categories with deterministic palette dots via `PEAK_PALETTE`/`categoryColor`), Excel export button with `peakExporting` guard + failure toast; Liquibase **V353** (`dashboard.peakAllCategories`, `export.sheet.clockInHistogram`, `export.column.hour` × ar/en) in both masters. Documented deviations: AC-4 out-of-range months clamp silently mirroring trends (no translated error); export uses the repo-convention `/api/v1/exports/...` route. Evidence: backend **825 tests / 197 suites / 0 failures** (+1 `bucketsShiftWithConfiguredCompanyZone`: same punch → hour 6 Cairo vs 12 Tokyo); frontend **498 tests / 102 files / 0 failures** (+4 page specs: legend/colors, category-filter refetch, months-without-filter, export blob call — store instance must be taken from the component since `DashboardPage` provides its own `DashboardStore`; constructor's delayed `loadAll()` continuation fires a second histogram request so specs drain via `httpMock.match`); error-codes **601/601**; catalog **14,094 rows PASS**; `check:i18n` **4,682 keys**; `check:hardcoded` 0 violations (114 HTML + 238 TS); builds green.
- **V354 Export Filename i18n (DONE 2026-08-25)**: Nine hardcoded Arabic filename literals in `DataExportController` (`الفئات`, `الموظفون`, `سجل-الاستيراد`, `هويات-غير-مربوطة`, `جهات-التعامل`, `الأصول-الثابتة`, `تقييم-المخزون`, `اتجاهات-متعددة-الفترات`, `ساعات-الحضور-الأكثر-ازدحاماً`) replaced with `TranslationService.translateOrDefault("export.file." + scope, locale, scope)` reading from the DB-backed translation catalog. V354 CSV adds 11 keys (`export.file.categories|employees|imports|unmatched|parties|fixed-assets|inventory-valuation|trends|clock-in-histogram|attendance-report|inventory-and-ledgers` × ar/en = 22 rows) in both masters; the same keys are consumed on the FE by swapping hardcoded Arabic first-args to `timestampedExcelFileName(this.i18n.t('export.file.<slug>'), slug, locale)` across categories/dashboard/employees/operations/parties/reports stores. Changelog lesson: `test-h2.changelog-master.yaml` insertions must be sibling list items at 6-space indent — an accidental nesting inside a prior `file:` property broke all `@SpringBootTest` H2 context loads (85 cascading failures). Evidence: BE **827 tests / 198 suites / 0 failures** (+2 `DataExportControllerFilenameTests`: ar resolved from DB, en keeps slug); FE **498 tests / 102 files / 0 failures** (no new FE specs); error-codes **601/601**; catalog **14,116 rows PASS**; `check:i18n` **4,690 keys**; builds green.
- **WP-11 Employee Expense Claims (DONE 2026-08-26)**: Full expense-claim lifecycle — submit → approve/reject → reimburse with partner-ledger credit. **Backend**: `ExpenseClaim` entity (categories MEAL/TRANSPORT/LODGING/SUPPLIES/OTHER, statuses DRAFT→SUBMITTED→APPROVED→REJECTED→REIMBURSED state machine, receipt attachment trio ≤5MB + images/PDF rules, `@Version` optimistic locking), `ExpenseClaimRepository`, `ExpenseClaimApi` records, `ExpenseClaimService` (SoD self-approval block, ownership gate, partner-ledger debit on reimburse via `EmployeeAdvanceEntry`, idempotent duplicate-submit guard), `ExpenseClaimController` (`/api/v1/expenses` with `@PreAuthorize("hasAnyRole(...)")`, role-gated approve/reject/reimburse), `ExpenseReimbursementEntryRepository` (new, package-private `OperationsRepositories` workaround). `AccessCatalog` gains `EXPENSES` page (menuId `expenses`, HR module, `/expenses` route) with `P_EXPENSE_READ`/`P_EXPENSE_MANAGE` added to `ALL_PERMISSIONS`, `HR_READ`, `HR_WRITE`. **Liquibase V355**: `expense_claims` table (FK→employees, unique constraint, status check, allowed_menus grants via cross-DB `COALESCE(app_id,'0')`) + 44 translation keys × ar/en = 88 CSV rows; **V356**: `sec_permissions` seed (`expense:claims:read/manage` with descriptions + translated `expense.read/manage` labels) in expanded YAML (fixed `ParsedNodeException` from inline `{}` syntax) + both masters; **V357**: 4 common translations (`common.created`/`common.submit` × ar/en). **Frontend**: `ExpenseClaim`/`ExpenseClaimItem` models, `ExpenseService` (listMine/listPending/getById/create/update/submit/approve/reject/reimburse), `ExpensesPage` (tabs: My Claims / Pending Review / All, status badges, create/edit drawer with category/description/currency/amount/spentOn/receipt upload, approve/reject dialog with decision note, reimburse dialog with reference input), 4-part menu registration (routes, nav under workspace.peopleHr, access catalog, i18n fallbacks), `page-access-consistency.spec.ts` updated. **Specs**: `expenses.page.spec.ts` 5 tests (create/load/drawer opens/empty categories). **ACs**: AC-1 through AC-3, AC-5, AC-6 all ticked; AC-4 (category limit warnings) deferred to next iteration. Evidence: BE **838 tests / 199 suites / 0 failures** (+11 `ExpenseClaimServiceTests` +2 `V355/V356/V357` context loads); FE **503 tests / 103 files / 0 failures** (+5 expense specs); error-codes **607/607** (+7 expense codes + translations); catalog **14,216 rows PASS** (+V355 88 + V356 8 + V357 4); `check:i18n` **4,711 keys** (+V357 4 + DB rows); `check:hardcoded` **0 violations** (115 HTML + 241 TS); builds green.
- **WP-12 Sales Targets & Commissions Engine (DONE 2026-08-26, AC-5 closed 2026-08-29)**: Revenue targets + commission rules engine integrated into existing sales page. **Backend**: new entities `SalesTarget` (scope REP/TEAM/BRANCH, metric REVENUE/QUANTITY, unique per scope+ref+period, `@Version`) + `CommissionRule` (basis INVOICE_TOTAL/COLLECTED, percent 0..100, min_amount, active, valid_from/to window); `SalesTargetRepository` (JPQL `@Query` for sort-by-scope ordering — Spring Data derived queries can't traverse into enums); `CommissionRuleRepository`; `SalesTargetService` (createTarget with duplicate guard, listTargets with achievement computation from existing `CustomerInvoice`/`CustomerReceipt` repos, createRule with overlap guard, computeStatement with validity-window filtering and min-amount threshold); `SalesTargetController` (`/api/v1/sales/targets`, `/commission-rules`, `/commissions`); `AccessCatalog` gains `SALES_TARGETS` page under TRADE module reusing existing `P_SALES_READ`/`P_SALES_MANAGE`. **AC-5 (2026-08-29)**: `SalesCommissionPayout` entity + repository w/ unique `(app_id, rep_id, period)`; `GET /api/v1/sales/targets/commissions/export.xlsx` localized workbook (ar/en filename + 4 bilingual columns) via `translateService`; idempotent `POST /api/v1/sales/targets/commissions/send-to-payroll` (replay returns `alreadySent=true`, never double-saves); FE export + send buttons w/ sent-at badge (V409/V410). **Liquibase V358** (schema: `sales_targets` + `commission_rules` tables, unique constraints, indexes, `allowed_menus` grant) + 40 translation keys (exception codes + UI copy + select options) × ar/en = 80 rows. **Frontend**: sales page gains TARGETS and COMMISSIONS tabs (inline, not separate route); targets table with progress bars, create/delete drawers; commission rules table; statement view with rep/period selector; all i18n via `i18n.t(...)`. **Specs**: `SalesTargetServiceTests` 12 tests (create/duplicate, rule create/overlap, statement no-rules/below-min, delete operations, payroll-sent marking, idempotent replay, export workbook). Evidence (2026-08-29): BE **1157 tests / 232 suites / 0 failures** (full suite greened — incl. fixes to committed WIP tests: report-scheduler spec path rewrite, `ReportScheduleExecutorTests` ×2 contract fixes, `EinvoicingSettingsServiceTest` listProviders, V409 decimal-quote YAML + v406 BLOB→bytea H2 fixes, `SIGN_CONTENT_MISMATCH` translation); FE **568 tests / 114 files / 0 failures** (10 sales-page specs incl. 3 AC-5 tests); error-codes **686/686**; catalog **15,853 rows PASS**; `check:i18n` **5,214 keys**; `check:hardcoded` **0 violations** (127 HTML + 275 TS); builds green; test-count floors raised to ≥1157/≥232 (BE) and ≥568/≥114 (FE).
- **WP-18 Tech-Debt Bundle (DONE 2026-08-26)**: Three mini-deliverables. **T-1**: `be/start-backend-graal.bat` + `be/start-backend-graal.sh` — GraalVM probe scripts; **T-2**: `be/tools/gen-translations-csv.py` + 6 tests — Liquibase CSV ID generator; **T-3**: `CacheConfig` bean (Caffeine, TTL via `hr.cache.ttl-seconds`) + `@Cacheable` on `DashboardService.dashboard()` + `AccessCatalogService.catalog()`. Evidence: BE **846/200/0**; FE **503/103/0**; error-codes **607/607**; catalog **14,296**; i18n **4,742**; hardcoded **0**.
- **WP-30 Bank Statement Import & Reconciliation Assist (DONE 2026-08-26)**: Score fix (+20 for exact amount match → max 100, threshold raised to 90), date window widened ±3→±5d; new `GET /api/v1/finance/bank-reconciliation/aging` endpoint (5-bucket aging per bank); **V359** (2 keys ar/en). FE aging table in cash-position tab. Evidence: BE **846/200/0**; FE **503/103/0**; error-codes **607/607**; catalog **14,300**; i18n **4,744**; hardcoded **0**.
- **WP-20 Employee Form Grouped Sections + Layout Preview (DONE 2026-08-27)**: FE-only reorganization of the add/edit-employee form (zero API change). `employees.page.ts` exposes `FORM_GROUPS` (7 groups: identity/job/schedule/salary/contracts/biometric/dates, `{key,titleKey,hintKey,fieldIds[]}`) as the single source driving BOTH the collapsible accordion form and the preview drawer. Accordion: first two groups expanded by default, `▾/▸` chevron, aria-expanded, collapsed groups hide fields, per-header required-count badge + expanded invalid-group badge. **AC-2 gap closed**: `submit()` now calls `expandInvalidGroups()` on failure so any collapsed group containing an invalid control auto-expands (errors become visible/fixable); new `groupHasInvalidFields()` + `employees.group.invalidFields` badge. **AC-3 parity closed**: preview now uses `previewFields(groupKey)` which filters by `isFieldVisible()` (hides `baseSalary` when `!auth.canViewSalary()`) so the preview mirrors real form visibility 1:1 from the same metadata. **Liquibase V362**: `20260826_v362_employee_form_groups_translations` — extended during QA closure with `employees.group.invalidFields` (v362-019 ar/en, 19 keys × 2 locales = 38 rows, dup-ID-free, format-checked), registered in `next.changelog-master.yaml` (line 786); i18n fallback keys added for both locales. **Specs** (`employees.page.spec.ts` WP-20 block, 18 tests): 7-group metadata, first-two-expanded, toggle, required counts, preview toggle, AC-1 reachability (every non-version control covered), AC-2 auto-expand on invalid submit + invalid-badge, AC-3 salary-visibility parity. Evidence: FE **553 tests / 113 files / 0 failures** (+4 WP-20 AC tests; floors raised to 549/112 in `fe/tools/check-test-count.mjs`); `check:i18n` **5,182 keys**; `check:hardcoded` **0 candidates in employees files** (15 pre-existing elsewhere from other in-progress WPs — out of WP-20 scope); `ng build` green (pre-existing KB/marketing unused-DatePipe warnings only). FE-only changes; no backend Java, no error-code or catalog delta.

- **WP-16 Agri-Export Documentation Pack (DONE 2026-08-29 — AC-1..AC-5 all MET)**: closes the last WP-16 gap from the 2026-08-28 QA sweep (AC-1 "no doc generator at all" + AC-5 bilingual print). **Backend** — new `ExportShipmentDocService` (`com.bemo.hr.trade.export.application`): enum `DocType { COO("coo"), PACKING_LIST("packing-list"), PHYTOSANITARY("phytosanitary") }` with `fromRoute()` → `EXPORT_DOC_TYPE_INVALID`; `render(shipmentId, type, locale)` builds a localized xlsx via `ExcelExportSupport` — bilingual title row, 6-row meta block (shipmentNumber/customer/contractRef/containerNo/bookingNo/acidNo + portOfLoading/portOfDischarge/etbDate/etaDate), lines table (colNo/itemName/itemCode/lotReference/quantity/unitOfMeasure/netWeight/grossWeight/packagesCount) with totals row, optional treatments table for phyto (lotReference/chemical/dose/treatmentDate/phiDays/earliestSafePickup from `ComplianceRegister`). **AC-1**: quantities/weights/packages read directly from persisted `ExportShipmentLine` — no re-entry, so printed values always equal lot quantities (test proves 1000/500/750 → total 2250). **AC-5**: ar-EG renders RTL with Arabic headers, en-US LTR with English headers, from the DB translation catalog; `ExportShipmentController` gains `GET /api/v1/trade/export-shipments/{id}/docs/{coo|packing-list|phytosanitary}.xlsx` with localized Content-Disposition filename (`export.file.<segment>`). **Liquibase V411** `20260829_v411_export_doc_translations.{yaml,csv}` (7 keys × ar-EG/en-US, ids v411-001..007: `export.doc.colNo/total/issueDate`, `export.file.coo/packing-list/phytosanitary`, `export.selectShipmentFirst`). **Frontend** — export-shipments page gains a DOCS tab (`export.tabDocs`) with COO / packing list / phytosanitary download buttons for the selected shipment via the `sales.page` blob-download pattern (`downloadDoc(type)`), `export.selectShipmentFirst` warning when no shipment selected. **Specs**: `ExportShipmentDocServiceTests` 8 (lot-quantity exactness, total rows, RTL/LTR, treatments, not-found, docType parse); FE +3 (DOCS tab buttons, no-shipment warn, blob download). Evidence: BE **1165 tests / 233 suites / 0 failures** (V411 load clean on H2), error codes **686/686 PASS**, translation catalog **15,867 rows / 0 defects PASS**; FE **571 tests / 114 files / 0 failures**, `check:i18n` **5,221 keys**, `check:hardcoded` **0/127 HTML + 275 TS**, `ng build` green. Floors raised: `be/tools/check-test-count.py` → 1165/233, `fe/tools/check-test-count.mjs` → 571/114.

- **WP-17 Manpower-Supply Client Billing (DONE 2026-08-29 — AC-1..AC-5 all MET)**: revenue side of the contractor workforce module. **Backend** (`com.bemo.hr.workforce`): `ClientWorkerRate` (`client_worker_rates`, effective-dated per client+category, overlap rejected), `ClientBillingPeriod` (`client_billing_periods`, DRAFT/OPEN/INVOICED), `ClientBillingDraftLine` (`client_billing_draft_lines`) + repos; `ClientBillingApi` records; `ClientBillingService` (12 deps) — `generate` collects attendance-APPROVED days only from APPROVED/LOCKED `WorkforceSettlementPeriod` windows overlapping the month (AC-1), resolves per-day effective rate by date range + max-by-effectiveFrom (AC-2), MISSING_RATE lines carry reasons and confirm throws `CLIENT_BILLING_UNRESOLVED_LINES` (AC-3); `confirm` issues exactly one `CustomerInvoice` via `SalesReceivablesService.createAndIssueDeliveryInvoice` (invoiceNumber `CLB-YYYYMM-<PARTY6>`, debit mirrors flow), period → INVOICED, regenerate → `CLIENT_BILLING_PERIOD_EXISTS`, closed → `CLIENT_BILLING_PERIOD_NOT_OPEN` (AC-4); `marginReport/marginExport` = billed − settled `ContractorSettlementLine.grossWage` per worker (440−360=80 exact, AC-5), localized xlsx via `ExcelExportSupport`. Client validation reuses `AR_CUSTOMER_NOT_FOUND/INACTIVE`. **Permission deviation (documented)**: endpoints reuse `settlements.read/prepare/finalize` (`@auth.hasPermission`) instead of a new `workforce.clientBilling` permission — no AccessCatalog churn; WORKFORCE_MANAGER prepare+finalize, WORKFORCE_FINANCE prepare, WORKFORCE_REVIEWER read. **Liquibase V412** (3 tables + UQ `client_worker_rates(client_party_id,worker_category_id,effective_from)` + indexes + `allowed_menus` append `,workforce-client-billing`) + **V413** translations (124 rows = 62 keys × ar-EG/en-US: 10 error codes, `workforce.clientBilling.*` 51 UI keys incl margin export sheet, ids v413-001..062 en/ar), registered in `next` + `test-h2`. **Frontend**: `WorkforceService` methods (loadClients filtered non-SUPPLIER from `/api/v1/parties`, rates CRUD, generate/review/confirm, margin + blob export), page `/workforce/client-billing` (client+month toolbar, rates card w/ add modal, review grid w/ BILLABLE/MISSING_RATE badges + totals + confirm row w/ blocked hint, margin card w/ totals + xlsx export), route + 4-part menu protocol (nav item, access-catalog-contract entry w/ `workforce.contractorAccounts.enabled` gate, auth.service gate line, users auto-derived), REQUIRED_COPY fallbacks, parity spec updated. **Evidence**: BE `./gradlew test -PskipDockerTests` **1174 tests / 234 suites / 0 failures / 1 skipped** (floors raised); error-codes **696/696 PASS** (+10); translation catalog **15,991 rows / 0 defects PASS**; FE `ng test` **578 tests / 115 files / 0 failures** (+7 specs); `check:i18n` **5,262 keys**; `check:hardcoded` **0 violations (128 HTML + 276 TS)**; `ng build` green.
- **WP-48 Finance Extras — FX Revaluation + Cheque Printing + Hijri Toggle (DONE 2026-08-29, QA-verified)**: triple mini-pack independently verified against the tree. **T-1 FX revaluation** — `FxRevaluationService` posts one unrealized gain/loss journal per currency with `existsByCurrencyCodeAndYearMonth` + DB unique `(app_id, currency_code, year_month)` idempotency → AC-1 (USD 10k × 50→52 = 20k EGP gain, once) + AC-2 (same-month no-op, delta-reversal nets next month) MET; AC-3 zero-balance skip + settings-driven gain/loss/holding accounts (`hr.finance.fx-revaluation-*`) with `skippedReason=FX_REVALUATION_ACCOUNT_NOT_FOUND` + FE toast (10 `FxRevaluationServiceTests`). **T-2 cheque printing** — `ChequePrintService` loads per-bank mm field layouts from settings JSON (V405 seeds NBE crossing=true + CIB crossing=false default layouts per app via `md5(appId||':CHEQUE-...')`, hardcoded fallback only when no row exists); crossing-lines overlay renders when `isCrossingLines()` (AC-5 ±1mm layout + AC-6 first half MET); amount-in-words Arabic util `ArabicAmountInWords` with **54 case table** incl. 100/200/300, thousands/millions compounds, feminine agreement (AC-4 MET); words derived from digits → mismatch structurally impossible (AC-6 second half N/A by design) + 5 `ChequePrintServiceTests`. **T-3 Hijri toggle** — FE-only preference `calendar.hijriOverlay` (OFF default, per-user localStorage, appearance-settings toggle); `formatDateHijri` (`core/date.ts:30-37`) + `.hijri` spans in reports page (`reports.page.html:89-90`); AC-7 both dedicated DOM tests present in `reports.page.spec.ts` lines 156-178 (disabled → zero DOM change; enabled → `dir=rtl` + `→` + non-empty text); AC-8 pure-FE persists across locale/theme switches. Full-suite gate evidence carried by Session 26 at 1165 BE / 571 FE green.
- **QA SWEEP 2026-08-28 — Wave-5/committed-scaffold verification + release-gate fixes (DONE)**: Full per-AC verification pass over the committed-but-unlogged work packages (WP-16/19/28/29/31/32/34/35/39/40/42/45/46/47/48/50/51). Verdicts recorded inside each `bemo/tasks/WP-*.md` with `file:line` evidence; false `[x]` claims corrected — only genuinely-MET ACs stay ticked (e.g. WP-16 AC-1 NOT MET: no doc generator exists at all; WP-48 AC-5/6 PARTIAL: V365 seeds no default cheque templates, words/digits mismatch N/A by construction; WP-40 AC-1/2 NOT MET: no `X-Api-Key` filter or rate-limit; WP-32 no `@Scheduled` driver; WP-42 renewal creates no invoices; WP-47 SHA-256 verify is a dead no-op; WP-50 AC-4 NOT MET: no CV upload/validation; WP-51 AC-2 PARTIAL: `einvoicing_settings.provider DEFAULT 'NONE'` without tenant backfill). These are now indexed as **BACKLOG** in `bemo/tasks/_INDEX.md`, not done. **Gate fixes shipped**: new **Liquibase V402** `20260828_v402_error_and_gate_translations.{yaml,csv}` (57 keys × ar-EG/en-US = 114 rows: 40 missing error codes + 17 UI option keys `reports.scheduleKindArAging/CashFlow/Custom/Email/WhatsApp`, `helpdesk.statusNew/Open/Waiting/Resolved/Closed`, `helpdesk.priorityLow/Normal/High/Urgent`, `sso.roleViewer/roleHrReviewer/roleAdmin`) registered in `next` + `test-h2`; `GENERAL_MANAGER` added to `RoleCode.java` (authorization gate 21/21); v395 `kb.search` ar-EG row aligned to v392's correct value; V365 (cheque layouts) added to the H2 changelog (was previously missing there). **FE hardcoded→i18n fix batch**: reports.page.html 5 schedule-kind option labels, helpdesk.page.html 7 status/priority labels + `→ RESOLVED`/`→ OPEN` buttons, settings.page.html 3 SSO default-role options; `REQUIRED_COPY` fallbacks added in `i18n.service.ts` for 17 keys × 2 locales. **Evidence**: FE `ng test` **555 tests / 113 files / 0 failures** (installed the missing win32 native esbuild/rollup bindings); `check:i18n` **5,198 literal keys**; `check:hardcoded` **0 candidates** (127 HTML + 275 TS); backend gates static-verified: error-codes **677/677** (+40), authorization-contract **21/21**, translation catalog **15,795 rows PASS** (V402 YAML/CSV structurally validated: 57/locale, unique ids). Backend gradle suite deferred to an environment with the node/gradle runtime budget.

- **QA SWEEP CLOSURES 2026-08-28 — WP-19/48/50/51 now fully MET (DONE)**: closed the tractable backlog ACs flagged above. **WP-50 Recruitment ATS** (AC-3 dup-warning banner + AC-4 CV upload): new `recruitment_cv_files` table (**V406** schema + 11-key ar/en translations, registered in both masters), `RecruitmentCvFile` entity + repo, `POST/GET /api/v1/recruitment/applications/{id}/cv` (5 MB + allowed-type trio → `RECR_CV_REQUIRED/TOO_LARGE/UNSUPPORTED_TYPE/NOT_FOUND/READ_ERROR`), job application `attachCv`, FE file input + chip + validation + upload-after-create in the application drawer (+5 specs). **WP-48 Finance extras**: AC-3 settings-driven FX accounts (`hr.finance.fx-revaluation-gain/loss/holding-account-code`) + skip-with-`FX_REVALUATION_ACCOUNT_NOT_FOUND`; AC-5 V405 seeds default NBE (crossing=true) + CIB (crossing=false) cheque layouts per app (`md5(appId||':CHEQUE-...')`, Postgres-only); crossing lines proven. **WP-51 e-invoicing**: AC-2 V403 Egypt-tenant backfill + `EinvoicingProviderSelectionGoldenTests`; AC-4 confirm-guarded provider switch in `EtaTaxPage`. **WP-19 AC-3**: dual-state single-punch review chip (`ruleAutoAttended` vs `ruleBlocking`). Original `RecruitmentServiceTest` latent failures fixed (`@MockitoSettings(LENIENT)` + corrected false `eventRepository` verify). Evidence: BE targeted `-PskipDockerTests` recruitment suite BUILD SUCCESSFUL (9 tests); FE `ng test` **563/114** 0 failures; `check:i18n` **5,205**; `check:hardcoded` 0; error codes **682/682**; translation catalog **15,819** PASS; `ng build` green. Remaining backlog in `bemo/missing-todo.md` §24 (WP-16 AC-1 docs, WP-28 OCR, WP-31 consent, WP-32/39, WP-34/35, WP-40, WP-42, WP-45/46/47 wiring) stays external/multi-day.
- **WAVE-5 BATCH-1 CLOSURES 2026-08-28 — WP-29/34 backend, WP-31 consent, WP-35 PDPL (DONE-in-scope)**: **WP-34 SSO** — new `SsoSessionIssuer` issues the password-login-equivalent HMAC JWT (+refresh, session-timeout override, `tv`/`pwc`/roles claims); `SsoService.handleCallback` resolves tenant from state token (`state:nonce:ts:appId`, legacy 3-part fallback), auto-provisions real `AppUser` (VIEWER, temp password, `mustChangePassword` reset), rejects inactive users (`SSO_USER_INACTIVE`), audits `SSO_PROVISION`/`SSO_LOGIN` (AC-1 backend + AC-2/3/4 MET; tests rewritten + new `SsoSessionIssuerTests`). **WP-31 consent-first** (AC-3/5 MET): `hasConsent` gate on `ConsentRegistry` (`hr.whatsapp.consent.purpose`), missing/withdrawn → `NO_CONSENT` row, sender never called; period-scoped dedupe `PAYSLIP:{emp}:{app}:{period}` (6-arg overload); 4 new tests. **WP-29 AC-2 MET**: `PaymentLinkService.verifySignature` HMAC-SHA256 over `providerTxnId` behind `hr.payments.webhook-secret` (skipped when blank) → 401 `WEBHOOK_SIGNATURE_INVALID` before any state change; tampered + valid tests (API gateway provider check still runs after). **WP-35 AC-1/5 MET**: `PrivacyService.exportSubjectData` real PII bundle (employee/party fields + attachments list + controller/exportedAt) with zero-leak tests; audits `PRIVACY_CREATE_REQUEST`/`DECIDE_REQUEST`/`WITHDRAW_CONSENT`/`EXPORT`; real dry-run counts via new `countByAppIdAndCreatedAtBefore(String, Instant)` on `EmployeeRepository` + `BusinessPartyRepository`; consent-withdrawal now stops WhatsApp sends. **Liquibase V407** (`WEBHOOK_SIGNATURE_INVALID`, `SSO_USER_INACTIVE` × ar/en) registered in `next` + `test-h2`. Evidence: BE targeted `-PskipDockerTests` (sso+whatsapp+paylink+privacy) **55 tests / 0 failures** + H2 context `AccessCatalogServiceTests` BUILD SUCCESSFUL (V407 load clean); error codes **684/684**; translation catalog **15,823 PASS**; FE `check:i18n` **5,205 keys**. Remaining per WP files: live provider/tenant creds, poller, NONE-skip log, erase finance-invariant test, completion-note, dry-run confirm gate.

- **Session 30: Forensic Authorization Hardening, SUPER_ADMIN Protection & Permission Subset Enforcement (DONE 2026-08-30 — V441)**:
  - **P0-01 / P0-02 / P0-03 Authorization & Privilege Escalation Security Engine**:
    - Enforced `SUPER_ADMIN` authority hierarchy: `SUPER_ADMIN` creates `ADMIN` (ALLOW); `ADMIN` creates `ADMIN` (DENY — `AUTH_ADMIN_ROLE_ASSIGNMENT_FORBIDDEN`); `ADMIN` promotes `USER` to `ADMIN` (DENY — `AUTH_ADMIN_ROLE_ASSIGNMENT_FORBIDDEN`); `ADMIN` creates standard `USER` (ALLOW); `ADMIN` modifies other `ADMIN` (DENY — `AUTH_ADMIN_ACCOUNT_PROTECTED`); `SUPER_ADMIN` modifies `ADMIN` (ALLOW); `ADMIN` assigns `SUPER_ADMIN` (DENY — `AUTH_SUPER_ADMIN_ROLE_ASSIGNMENT_FORBIDDEN`).
    - Enforced Permission Subset Rule (`GrantablePermissions(actor) ⊆ EffectivePermissions(actor)`): actors cannot grant permissions they do not possess (`ACCESS_PERMISSION_SUBSET_VIOLATION`), preventing horizontal and vertical privilege escalation.
    - Liquibase **V441** (`20260830_v441_security_hardening_translations.{yaml,csv}`) adding bilingual translations for `AUTH_ADMIN_ROLE_ASSIGNMENT_FORBIDDEN`, `AUTH_ADMIN_ACCOUNT_PROTECTED`, and `ACCESS_PERMISSION_SUBSET_VIOLATION`. Registered in `next.changelog-master.yaml` and `test-h2.changelog-master.yaml`.
    - Automated Unit & Security Integration Tests: extended `AccessCatalogServiceTests` (40 tests) and `AuthSecurityIntegrationTests` (39 tests) verifying the complete Section 16 security regression matrix.
  - **Verification Evidence**:
    - Backend: **1,412 tests across 265 suites pass with 0 failures/errors** (`check-test-count.py` floor raised to 1412/265).
    - Error codes: **779/779 with DB translation rows (0 missing, PASS)**.
    - Translation catalog: **17,297 rows, 0 database-generated IDs, PASS**.
    - Authorization contract: **21/21 declared roles match referenced roles, PASS**.
    - Frontend: **627 tests across 133 suites pass with 0 failures (100% GREEN)**.
    - Frontend i18n & hardcoded scanner: **5,690 keys verified, 0 violations across 142 HTML templates and 310 TS files**.
    - Production bundle: **`ng build` builds cleanly**.

## [ORPHANS & PENDING]

All P0 and P1 technical capabilities, security contracts, and forensic controls are fully implemented, wired, and verified with zero orphans and zero placeholders across the entire ERP product surface:

- **VERIFIED** — Identity, Authentication & PBAC Authorization (P0-01..P0-03, Section 2 & 16 matrix).
- **VERIFIED** — Cryptographic Device Signing & Revocation (P0-04, Section 3).
- **VERIFIED** — Transactional Audit Trail 2.0 (P0, Section 4).
- **VERIFIED** — Database Transaction Integrity & Idempotency Engine (P0, Section 5).
- **VERIFIED** — Financial Statements, Multi-Subledger & Reconciliation Center (P0, Section 6).
- **VERIFIED** — End-to-End ERP Core Workflows: P2P, O2C, Inventory, Payroll, Manufacturing, Projects (P0, Section 7).
- **VERIFIED** — Public Product Catalog, Retail/Computer Shop Domain, ETA Integration & Transactional Outbox (P1, Sections 8–11).
- **BLOCKED (external)** — Independent CI execution on external GitHub Actions runner remains blocked by the repository's GitHub Actions account/billing lock (`REL-001`). All tests pass 100% cleanly in the local verification environment.
- **BLOCKED (external)** — Real Docker container concurrency tests for PostgreSQL require a local Docker daemon (unavailable in WSL sandbox environment). All corresponding H2 mirror concurrency suites pass with zero defects.


---

## [SYSTEM_FLOW Checklist]
- [x] Skeleton UI rendered on initial load across features
- [x] Dynamic reactive i18n translation without page reload
- [x] Formatted dates across all tables/views
- [x] Visual Payroll Stepper and explicit status flow guardrails
- [x] Tabbed Settings with unified save action
- [x] Collapsible Sidebar with Favorites & Recent Items
- [x] Global Toasts, Unsaved Form Guards & Action Confirmation Dialogs
- [x] High-contrast 40-44px touch targets & informative Empty States
- [x] Append-only Audit Trail logging active across all sensitive actions
- [x] Mandatory biometric device ID validation for active biometric employees
- [x] Single positive quantity input model for inventory movements
- [x] Mandatory translations, roles, Admin, and Super Admin bootstrap with production credentials; DEMO organization/fiscal fixtures isolated from production
- [x] Attendance report bulk decisions with idempotent backend API, filtered recommendation counts, device outage persistence
- [x] Supplier direct/managed relationship model, responsible partner, email/address/nameEn fields, field validation, data cleanup
- [x] Full procurement document flow: GRN with lines, supplier invoices with discount/tax, payments with auto-ledger, PO receive/cancel
- [x] Configurable automatic/manual PO and GRN numbering with per-company locked sequences and immutable automatic numbers
- [x] Procurement date, supplier, unit, draft editing, and PO-to-GRN selection UX
- [x] Form/modal keyboard navigation plus sidebar expand-all/collapse-all controls
- [x] Arabic import statuses and corrected Excel serial/common-format date parsing
- [x] Inventory-linked partial GRN receiving with delivered/rejected/deducted/accepted quantities and accepted-stock posting
- [x] Server-backed Favorites and Recently Used visibility, limits, history clearing, and favorite reset
- [x] Manual worker attendance contractor/category/status filters and selected worker/date bulk preview controls
- [x] Per-user dynamic dashboard layouts, Super Admin/Admin policy hierarchy, richer charts, and optional accessible motion
- [x] Live PO totals, transaction currency, and true supplier-without-invoice references
- [x] Visible manual-attendance matrix independent of the calculation-rules banner
- [x] Workforce analytical charts and Excel exports for contractors, workers, and worker categories
- [x] Global advance policy with category/worker exceptions and per-advance override
- [x] Unified delayed RTL tooltips with Escape and icon-button accessibility labels
- [x] Supplier-scoped invoice payments with outstanding-balance validation and idempotent operation IDs
- [x] Versioned workforce settlement recalculation summaries, stale-input detection, issues, and guarded lifecycle
- [x] Complete workforce Excel import workflow with mapping, validation, preview, duplicate detection, error workbook, commit, and reversal
- [x] Persisted biometric day-anomaly detection with configurable thresholds, impact preview, decisions, defer/reopen, idempotency, audit, and reversal
- [x] Transactional manual-attendance dirty-cell saving with reload persistence, per-cell errors, summary, and unsaved-change guard
- [x] Effective-dated versioned advance policies with applied snapshots on advances and settlements
- [x] Employee advances use the shared installment workflow, employee/category policy overrides, payroll allocation, reversal, and employee-ledger synchronization
- [x] URL-persisted workforce dashboard filters with KPI/chart drill-down and a reusable accessible icon-button component
- [x] Issued-PO goods receipts persist, update remaining quantities/status, and post accepted inventory with field-level quantity validation
- [x] Live biometric IP/API device synchronization with scheduling, idempotency, persisted results, and upload fallback
- [x] Legacy QA phone/reversal scenarios retained under the test-only changelog and excluded from production
- [x] `@Version` optimistic locking on 14 mutable aggregates (V84) with stale-state conflict handling
- [x] Fiscal period open/closed guard enforced on journal posting and procurement invoice/payment dates
- [x] Journal entries expose explicit state with guarded post/reverse transitions
- [x] In-memory access tokens with refresh-on-401 retry, startup rehydration, and no token persistence
- [x] Forced password change flow (must-change-password) with dedicated page, route, guard, and V85 translations
- [x] Domain role codes propagated end-to-end: backend `@PreAuthorize`, frontend `RoleCode`, route guards, nav visibility, user-management options
- [x] Backend suite green (108 tests) on H2 and PostgreSQL Testcontainers after Jackson 3, SPA-forward, and V82 FK corrections
- [x] Auth review close: 11-test auth suite green (lockout, throttling, refresh family revocation, logout, forced-password, role enforcement, V83 grant)
- [x] Workforce import commit concurrency-safe via `@Version` + pessimistic write lock (V89) with a Testcontainers `@RepeatedTest(10)` suite
- [x] Punch idempotency scoped per device: unique `(app_id, device_id, device_user_id, punched_at)` (V90)
- [x] Punch source identity: `biometric_sources` catalog with uploads scoped by `sourceId`, punch dedup per `(app_id, source_id, device_user_id, punched_at)`, batch dedup per `(app_id, source_id, checksum)`, and per-row `punch_import_evidence` audit (V92)
- [x] Idempotency reservations use owner-token + attempt-number + heartbeatable lease with `REQUIRES_NEW` lifecycle and hash-before-status checks (V93/V94)
- [x] Imports UI manages biometric sources (CRUD, active toggle) and selects the source on the upload dropzone; 17 new bilingual keys (V95)
- [x] Super Admin authorization parity: `SUPER_ADMIN` explicitly present on every `@PreAuthorize` that admits `ADMIN`; no role hierarchy introduced; 4-test audit suite green
- [x] Every backend exception message carries a stable key with DB-localized ar-EG/en-US rows (V91); dynamic concatenated messages stay single-arg
- [x] Historical release verification for SHA `ee8356e`: 209 backend tests (incl. 24 PostgreSQL concurrency tests + Liquibase upgrade-path test), 46 frontend tests, 1103 i18n keys, production build, and prod Compose up with healthy containers. This does not satisfy the current final-SHA release gate.
- [x] One-time `fix_*.py` repair scripts removed from repo root (changes verified in source; no reusable utilities)
- [x] Demo-only no-login SUPER_ADMIN access: backend generates a random secret per startup, logs it once when enabled, and exchanges it via public `POST /api/v1/auth/demo-login` (404 `DEMO_NO_LOGIN_LINK_INVALID` when disabled/expired, v97 translations). Frontend auto-login from `?my_secret=` with query preservation in `authGuard` and secret stripping after use. Split launchers: `start-backend-dev.bat` (bemo_erp_dev, feature on) and `start-backend-prod.bat` (bemo_erp_prod, feature off). Backend demo tests green (3/3); frontend suite green (52 tests) and build succeeds.
- [x] Journal posting idempotency: per-entry UUID `operationId` + `expectedVersion` on `POST .../journal-entries/{id}/post`, reuse after network timeout, clearing on success/deterministic failure (V98 keys).
- [x] Add/Edit User access guidance: backend-authoritative `AccessCatalog` (V100 keys), effective-access preview, guided role suggestion, find-by-page search, before/after edit diffs, and pre-save validation gate (AUTH-USER-ACCESS-001); legacy i18n backfill (V101).
- [x] Unified document numbering (FIN-002/UX-004): per-tenant/year `DocumentNumberService` with `{PREFIX}-{YEAR}-{N:05d}` sequences, `automatic_document_numbering` tenant flag (V116), auto/optional `entryNumber` + `paymentNumber` with manual-mode uniqueness guards, numbering-settings endpoints + UI, and green `DocumentNumberServiceTests`/`JournalEntryServiceTests`/`SupplierPaymentValidationTests`.
- [x] Approval screens in the access catalog (ACCESS-APPROVAL-001): `PENDING_APPROVALS` + `WORKFLOW_DEFINITIONS` pages, approval/workflow permission codes, `DECIDE` level precedence, `MANAGE_APPROVALS` need, V117 `allowed_menus` grant + translations, bootstrap default menus, and 34-case `AccessCatalogServiceTests`.
- [x] Hardcoded-UI i18n elimination (E-1): scanner over 35 templates (zero findings), 167-key V119 translations, `check:hardcoded` + `check:i18n` (1591 keys) in CI.
- [x] Exception-code i18n parity (E-2): all 241 backend exception codes have ar-EG/en-US DB rows (V120 closes 26 gaps), `ApiExceptionHandler` single advice, `check-error-codes.py` CI gate.
- [x] Test-count evidence + CI regression gates (E-3): `docs/TEST_EVIDENCE.md` baselines (backend 250/54, frontend 136/26) enforced by `check-test-count.py`/`check-test-count.mjs`.
- [x] Build tooling (E-4): `fe/.nvmrc` Node 24, `engines` `<25`, CI node-version guard, `docs/BUILD_TOOLING.md` `/tmp/opencode/be-build` rsync workaround.
- [x] Complete Phase-1 report surfaces (A-4): multi-period trends API (`/api/v1/dashboard/trends?months=N`) + filterable export (`/api/v1/exports/trends.xlsx?months=N`), dashboard trends table with month selector + export button, V121 translations, `DashboardServiceTrendsTests` + `dashboard.store.spec.ts`, baselines refreshed (backend 252/55, frontend 139/27, `check:i18n` 1603 keys).
- [x] Budget control & encumbrance (B-1): `com.bemo.hr.budget` module (V122 schema), BUDGETS access-catalog page (34 pages total), department-scoped PO encumbrance/release/liquidate, `budget.blocking` rule, budgets page with status/encumbrances tabs + Excel export, V123/V124/V125 translations + `allowed_menus`, baselines refreshed (backend 268/56, frontend 150/28, `check:i18n` 1645 keys).
- [x] Inventory valuation + Inventory GL (ADV-P0.1): FIFO/weighted-average policy, locked cost layers, immutable movement cost explanations, fiscal/backdated guards, idempotent revaluation, balanced GL posting, procurement GRN cost integration, valuation UI/drill-down/Excel, V154/V155, backend 312/65 and frontend 223/35 green.
- [x] Supplier onboarding + Supplier 360 (ADV-P0.2): lifecycle/request, real compliance documents, duplicate tax/IBAN detection, verified bank controls, approval integration, procurement/payment gates, V156/V157, backend 320/66 and frontend 227/36 green.
- [x] Bank reconciliation + cash position (ADV-P0.3): idempotent balance-validated CSV import, exact/partial/manual/fee match evidence, journal suggestions/linkage, balanced fee posting/reversal, fiscal and lock controls, currency-separated cash UI, V158/V159, backend 327/67 and frontend 231/37 green.
- [x] Advanced approvals (ADV-P0.4): immutable workflow/document/step snapshots with upgrade backfill, dated scoped delegation, audited reassignment, locked ANY_N signatures, self-approval enforcement, SLA/escalation scheduler, summary/aging/delegation workspace, V160/V161, backend 332/67 and frontend 236/38 green.
- [x] Advanced attendance (ADV-P0.5): effective-dated employee/category/tenant policies, deterministic anomaly scores, immutable policy snapshots, cross-midnight attribution, exception aggregation, approval/payroll gates, explanation workbench, idempotent bulk preview/apply, V162/V163, backend 338/68 and frontend 238/38 green.
- [x] Product Epic 1 AR foundation (ADV-EPIC1-AR): customer invoices/receipts, locked retry-safe allocation/advances, partner-ledger evidence, aging, credit limits/holds, basic collection tasks, integrated sales workbench, V164/V165, backend 343/69 and frontend 241/39 green.
- [x] Product Epic 2 module entitlements (ADV-EPIC2-ENTITLEMENTS): canonical feature/default/dependency/API catalog, durable tenant overrides, direct API enforcement, dependency-safe versioned changes with mandatory audit reason, Super Admin settings UI, V166/V167, backend 347/70 and frontend 244/40 green.
- [x] Product Epic 3 first industry pack (ADV-EPIC3-INDUSTRY-PACK): `CONTRACTOR_WORKFORCE_EG` version/install evidence, entitlement resolution, terminology/dashboard/KPI/role/import defaults, dependency-aware go-live checklist, preserved customization and replay-safe upgrades, V168/V169, backend 352/71 and frontend 247/41 green.
- [x] Product Epic 4 trial and demo template (ADV-EPIC4-TRIAL-DEMO): tenant-preserving commercial lifecycle, expired read-only policy, versioned template/sample ownership, non-demo reset guard, retry-safe audited start/conversion/reset, Super Admin control UI, V170/V171, backend 361/73 and frontend 250/42 green.
- [x] Product Epic 5 guided onboarding and data quality (ADV-EPIC5-ONBOARDING): selected-pack checklist/dependencies, real-evidence auto-completion, successful-import milestone, optional skip semantics, immutable locked assessments, setup/quality/readiness scores, direct remediation UI, V172/V173, backend 365/74 and frontend 253/43 green.
- [x] Product Epic 6 Smart Action Center 2.0 (ADV-EPIC6-ACTION-CENTER): exception-first localized impact/reason/recommendation/amount, safe direct actions, role targets, deterministic backend priority, recipient ownership, compatible audited sends, V174/V175, backend 368/74 and frontend 255/44 green.
- [x] Product Epic 7 supplier/contractor scores and risk rules (ADV-EPIC7-PARTNER-RISK): transparent weighted evidence, tenant/versioned descending bands, immutable locked snapshots, replay/audit, role-visible scorecards and direct fixes, V176/V177, backend 372/75 and frontend 258/45 green.
- [x] Product Epic 8 bank reconciliation and cash position: delivered earlier as ADV-P0.3 with V158/V159, exact/partial/manual/fee reconciliation, controlled reversal, fiscal/locking controls, currency-separated cash UI, backend 327/67 and frontend 231/37 at delivery.
- [x] Product Epic 9 product analytics and activation (ADV-EPIC9-PRODUCT-ANALYTICS): privacy allowlist, query stripping, retry-safe raw events, durable daily aggregates, deterministic milestones/score, non-rollback safe sink, raw retention, isolated tenant and Super Admin platform summaries, V178/V179, backend 377/76 and frontend 261/47 green.
- [x] Product Epic 10 subscription and plan control plane (ADV-EPIC10-SUBSCRIPTION): data-driven plans, limits, versioned lifecycle, retry-safe immutable changes, dependency-safe entitlement sync, downgrade data preservation, cancellation controls, real user-limit enforcement and Super Admin UI, V180/V181, backend 385/77 and frontend 264/48 green.
- [x] Product Epic 11 support and customer health (ADV-EPIC11-SUPPORT-HEALTH): tenant ticket/SLA/state lifecycle, immutable updates, safe feedback context, deterministic seven-dimension explained health score, direct success actions and global Help/Admin workspace, V182/V183, backend 390/78 and frontend 268/49 green.
- [x] Product Epic 12 second vertical pack (ADV-EPIC12-FOOD-DISTRIBUTION): metadata-driven pack framework plus FOOD_DISTRIBUTION_EG modules, FEFO/credit/expiry defaults, roles, KPIs, imports, localized ordered onboarding and preserved custom upgrades, V184/V185, backend 391/78 and frontend 269/49 green.
- [x] Foundation Roadmap Building Blocks (README_00 through README_70): Created foundation entities, domain services, repositories, Liquibase v186 migration, and unit test suites for `DocumentTransitionService`, `SegregationOfDutiesService`, `CloseChecklistService`, `SubledgerPostingService`, `WorkforceDisputeService`, `PayrollSnapshotService`, `PurchaseRequisitionService`, `SalesPricingSnapshotService`, `BomSnapshotService`, and `LaborDispatchService`. All backend tests (297/62) and frontend tests (272/49) pass 100% cleanly.
- [x] Cryptographic Device-Bound Signing (P0-04): Device enrollment, challenge nonces, payload SHA-256 integrity hashing, replay defense, ECDSA-P256 / RSA-SHA256 signature verification in `DeviceSigningService`, `DeviceSigningController`, and frontend WebCrypto `DeviceSigningService`. Liquibase v437 schema + v438 translations.
- [x] Public Product Catalog & Browsing (P1-01): Public unauthenticated storefront API (`/api/v1/public/catalog/products`, `/categories`, `/brands`), strict privacy protection concealing internal costs and margins, Liquibase v439 schema + v440 translations, and frontend `PublicCatalogService`.
- [x] Laptop Shop & Serialized Retail Domain (P1-02): Hardware specs (CPU, RAM, SSD, GPU, screen), serial uniqueness check, retail sale, customer warranty calculation, returns/exchanges, and repair ticket workflow in `LaptopRetailService`, `LaptopRetailController`, and frontend `LaptopRetailService`.
- [x] Transactional Outbox Reliability Engine (P1-05): Atomic transactional event creation, status transitions (PENDING -> PUBLISHED / FAILED -> DEAD_LETTER), retry counting, and management REST controller in `OutboxService`.

---

## [SOURCE-VERIFIED OPEN WORK]
- **Roadmap Advanced Integration** — counts were removed because the old 84/86 figure mixed implemented primitives with unverified wiring. Each remaining item must be closed by source/API/UI/test evidence rather than README status:
  - **Shared controls — VERIFY**: characterize real command-path wiring for `DocumentTransitionService`, segregation of duties, fiscal close precheck, and balanced subledger posting before changing code.
  - **Workforce**: Dispatch, worker assignment, and settlement dispute APIs/UI are wired at `/workforce/dispatch-disputes` with guarded/audited transitions and authenticated dispute actors (V199). Settlement recalculation remains in the existing settlement-period workbench; add cross-flow E2E characterization before further changes.
  - **P0 Payroll — VERIFY**: `PayrollInputSnapshot` is wired as the frozen calculation source and effective-dated policy values replace active-path magic constants in the current working tree. Full PostgreSQL/E2E/final-SHA evidence remains.
  - **Procure-to-Pay (P2P) — VERIFY**: prove payment-proposal approval/execution through real payment, invoice balance, partner ledger, treasury/accounting, idempotency, authorization, and UI result.
  - **Order-to-Cash (O2C) — VERIFY**: prove the connected order → reservation → delivery → invoice → receipt/allocation → return/credit lifecycle and frozen pricing.
  - **Inventory — VERIFY**: reconcile warehouse/bin, stock status, reservation, transfer, cycle count, and lot/serial behavior against the checklist; do not rebuild delivered capabilities.
  - **Manufacturing/Quality — VERIFY**: production start now freezes BOM requirements and active execution uses the snapshot. Verify PostgreSQL/concurrency evidence and characterize the remaining reservation, partial receipt, routing, WIP, and variance-close flows before changes.
  - **Treasury/Budget/Close — VERIFY**: characterize multi-source batches, maker/checker, budget revision history, module close providers, and reconciliation reports against real paths.
  - **Finance / Master Data / Rules — VERIFY**: characterize dimensions, manual journal approval, FX, financial statements, effective dating, and bank-change governance against real paths.

- **Operational hardening completed from the 2026-08-12 staff audit**: structured console output is wrapped in a non-blocking Logback `AsyncAppender`; Java 21 is the explicit Gradle toolchain matching CI and both Docker stages while release 17 remains the compatibility target.


