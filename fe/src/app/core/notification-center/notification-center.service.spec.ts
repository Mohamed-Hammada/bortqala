import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{NotificationCenterService}from'./notification-center.service';
describe('NotificationCenterService',()=>{let service:NotificationCenterService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(NotificationCenterService);http=TestBed.inject(HttpTestingController);});afterEach(()=>{http.verify();TestBed.resetTestingModule();});it('loads backend-ranked exception cards with business guidance',async()=>{const promise=service.loadNotifications();http.expectOne('/api/v1/notifications').flush([{id:'n1',recipientUsername:'admin',titleAr:'a',titleEn:'e',messageAr:'a',messageEn:'e',notificationType:'EXCEPTION',priority:'HIGH',exceptionKey:'OVERDUE',impactEn:'Cash risk',reasonEn:'Past due',recommendationEn:'Contact customer',actionLink:'/sales',roleTargets:['FINANCE_MANAGER'],priorityScore:110,isRead:false,createdAt:1}]);await promise;expect(service.notifications()[0].priorityScore).toBe(110);expect(service.notifications()[0].recommendationEn).toBe('Contact customer');expect(service.unreadCount()).toBe(1);});it('marks a card read without losing advanced fields',async()=>{service.notifications.set([{id:'n1',recipientUsername:'admin',titleAr:'a',titleEn:'e',messageAr:'a',messageEn:'e',notificationType:'EXCEPTION',priority:'HIGH',recommendationEn:'Act',roleTargets:[],priorityScore:85,isRead:false,createdAt:1}]);service.unreadCount.set(1);const promise=service.markAsRead('n1');http.expectOne('/api/v1/notifications/n1/read').flush({});await promise;expect(service.notifications()[0].isRead).toBe(true);expect(service.notifications()[0].recommendationEn).toBe('Act');expect(service.unreadCount()).toBe(0);});});
