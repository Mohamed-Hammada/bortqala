import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{ProductAnalyticsClient}from'./product-analytics-client.service';describe('ProductAnalyticsClient',()=>{it('strips query data and tolerates analytics failure',()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});const service=TestBed.inject(ProductAnalyticsClient);const http=TestBed.inject(HttpTestingController);service.captureNavigation('/reports?email=never-send-this');const request=http.expectOne('/api/v1/product-analytics/events');expect(request.request.body).toEqual(expect.objectContaining({eventName:'PAGE_VIEW',featureKey:'navigation',properties:{route:'/reports',source:'web'}}));expect(request.request.body.operationId).toBeTruthy();request.flush({}, {status:503,statusText:'Unavailable'});expect(()=>http.verify()).not.toThrow();TestBed.resetTestingModule();});});
