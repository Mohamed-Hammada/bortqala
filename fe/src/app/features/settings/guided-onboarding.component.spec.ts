import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{ComponentFixture,TestBed}from'@angular/core/testing';import{provideRouter}from'@angular/router';import{I18nService}from'../../core/i18n.service';import{NotificationService}from'../../core/notification.service';import{GuidedOnboardingComponent}from'./guided-onboarding.component';
describe('GuidedOnboardingComponent',()=>{let fixture:ComponentFixture<GuidedOnboardingComponent>;let component:GuidedOnboardingComponent;let http:HttpTestingController;const overview={packCode:'CONTRACTOR_WORKFORCE_EG',setupProgress:50,dataQualityScore:60,readiness:'BLOCKED',assessedAt:0,issues:[{code:'IMPORT',labelKey:'onboarding.issue.import',route:'/imports',count:0,blocker:true}],steps:[{key:'industryPack.step.company',sequence:1,optional:false,status:'COMPLETED'}]};beforeEach(async()=>{await TestBed.configureTestingModule({imports:[GuidedOnboardingComponent],providers:[provideHttpClient(),provideHttpClientTesting(),provideRouter([]),{provide:I18nService,useValue:{t:(k:string)=>k}},{provide:NotificationService,useValue:{success:vi.fn()}}]}).compileComponents();fixture=TestBed.createComponent(GuidedOnboardingComponent);component=fixture.componentInstance;http=TestBed.inject(HttpTestingController);http.expectOne('/api/v1/platform/onboarding/CONTRACTOR_WORKFORCE_EG').flush(overview);await fixture.whenStable();});afterEach(()=>{http.verify();TestBed.resetTestingModule();});it('loads progress quality blockers and steps',()=>{expect(component.overview()?.setupProgress).toBe(50);expect(component.overview()?.issues[0].route).toBe('/imports');expect(component.overview()?.steps[0].status).toBe('COMPLETED');});it('runs an operation-id assessment',async()=>{const promise=component.assess();const request=http.expectOne('/api/v1/platform/onboarding/CONTRACTOR_WORKFORCE_EG/assess');expect(request.request.body.operationId).toBeTruthy();request.flush({...overview,setupProgress:100,dataQualityScore:100,readiness:'READY'});await promise;expect(component.overview()?.readiness).toBe('READY');});it('maps backend readiness and step states to translation keys',()=>{expect(component.statusKey('IN_PROGRESS')).toBe('onboarding.status.in_progress');expect(component.statusKey('SKIPPED')).toBe('onboarding.status.skipped');});});
