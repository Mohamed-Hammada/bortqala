import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{ComponentFixture,TestBed}from'@angular/core/testing';import{provideRouter}from'@angular/router';import{I18nService}from'../../core/i18n.service';import{NotificationService}from'../../core/notification.service';import{AuthService}from'../../core/auth/auth.service';import{SupportPage}from'./support.page';
describe('SupportPage',()=>{let fixture:ComponentFixture<SupportPage>;let component:SupportPage;let http:HttpTestingController;const ticket={id:'t1',ticketNo:'SUP-1',priority:'HIGH',category:'BUG',moduleCode:'HR',screen:'/employees',businessImpact:'Blocked',description:'Issue',status:'NEW',assignedTeam:'SUPPORT',slaDueAt:1,createdBy:'user',createdAt:1,updatedAt:1,resolvedAt:0,version:0};const health={score:70,band:'WATCH',dimensions:{usage:20},reasons:[{key:'health.usage',points:20,status:'POSITIVE',actionRoute:'/settings'}],operationId:'h',calculatedAt:1,replayed:false};beforeEach(async()=>{await TestBed.configureTestingModule({imports:[SupportPage],providers:[provideHttpClient(),provideHttpClientTesting(),provideRouter([]),{provide:I18nService,useValue:{t:(key:string)=>key}},{provide:NotificationService,useValue:{success:vi.fn()}},{provide:AuthService,useValue:{hasAnyRole:()=>true}}]}).compileComponents();fixture=TestBed.createComponent(SupportPage);component=fixture.componentInstance;http=TestBed.inject(HttpTestingController);await flushLoad();await fixture.whenStable();});afterEach(()=>{http.verify();TestBed.resetTestingModule();});async function flushLoad(){http.expectOne('/api/v1/support/tickets').flush([ticket]);await Promise.resolve();http.expectOne('/api/v1/support/health').flush(health);}
it('loads tenant tickets and explained health',()=>{expect(component.tickets()[0].ticketNo).toBe('SUP-1');expect(component.health()?.reasons[0].key).toBe('health.usage');});it('creates an idempotent ticket with business impact',async()=>{component.impact='Payroll is blocked for all employees';component.description='Cannot post';const promise=component.create();const request=http.expectOne('/api/v1/support/tickets');expect(request.request.body).toMatchObject({priority:'MEDIUM',businessImpact:'Payroll is blocked for all employees',description:'Cannot post'});expect(request.request.body.operationId).toEqual(expect.any(String));request.flush(ticket);await Promise.resolve();await flushLoad();await promise;});it('sends privacy-limited feedback context',async()=>{component.feedbackType='RATING';component.feedbackMessage='Useful';component.rating=4;const promise=component.feedback();const request=http.expectOne('/api/v1/support/feedback');expect(request.request.body).toMatchObject({type:'RATING',message:'Useful',rating:4});expect(request.request.body).not.toHaveProperty('formValues');request.flush({id:'f'});await promise;});it('recalculates transparent health evidence',async()=>{const promise=component.calculate();const request=http.expectOne('/api/v1/support/health/calculate');expect(request.request.body.operationId).toEqual(expect.any(String));request.flush({...health,score:80});await promise;expect(component.health()?.score).toBe(80);});});
