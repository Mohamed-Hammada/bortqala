import{ChangeDetectionStrategy,Component,inject,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{RouterLink}from'@angular/router';import{HttpClient}from'@angular/common/http';import{firstValueFrom}from'rxjs';import{I18nService}from'../../core/i18n.service';import{NotificationService}from'../../core/notification.service';import{AuthService}from'../../core/auth/auth.service';import{apiErrorMessage}from'../../core/api-error';
interface Ticket{id:string;ticketNo:string;priority:string;category:string;moduleCode:string;screen:string;businessImpact:string;description:string;status:string;assignedTeam:string;slaDueAt:number;createdBy:string;createdAt:number;updatedAt:number;resolvedAt:number;version:number}interface Reason{key:string;points:number;status:string;actionRoute:string}interface Health{score:number;band:string;dimensions:Record<string,number>;reasons:Reason[];operationId:string;calculatedAt:number}type Tab='tickets'|'feedback'|'health';
@Component({selector:'app-support-page',imports:[FormsModule,RouterLink],templateUrl:'./support.page.html',styleUrl:'./support.page.scss',changeDetection:ChangeDetectionStrategy.OnPush})export class SupportPage{private readonly http=inject(HttpClient);readonly i18n=inject(I18nService);readonly auth=inject(AuthService);private readonly notifications=inject(NotificationService);readonly tickets=signal<Ticket[]>([]);readonly health=signal<Health|null>(null);readonly loading=signal(true);readonly saving=signal(false);readonly error=signal<string|null>(null);readonly tab=signal<Tab>('tickets');priority='MEDIUM';category='QUESTION';moduleCode='GENERAL';screen=location.pathname;impact='';description='';feedbackType='GENERAL';feedbackMessage='';rating=5;nextStatus:Record<string,string>={};comments:Record<string,string>={};constructor(){void this.load();}
async load(){this.loading.set(true);this.error.set(null);try{this.tickets.set(await firstValueFrom(this.http.get<Ticket[]>('/api/v1/support/tickets')));if(this.auth.hasAnyRole(['SUPER_ADMIN','ADMIN']))this.health.set(await firstValueFrom(this.http.get<Health|null>('/api/v1/support/health')));}catch(e){this.error.set(apiErrorMessage(e,this.i18n));}finally{this.loading.set(false);}}
async create(){if(this.saving())return;this.saving.set(true);this.error.set(null);try{await firstValueFrom(this.http.post('/api/v1/support/tickets',{priority:this.priority,category:this.category,moduleCode:this.moduleCode,screen:this.screen,businessImpact:this.impact,description:this.description,operationId:crypto.randomUUID()}));this.impact='';this.description='';this.notifications.success(this.i18n.t('support.created'));await this.load();}catch(e){this.error.set(apiErrorMessage(e,this.i18n));}finally{this.saving.set(false);}}
async update(ticket:Ticket){const status=this.nextStatus[ticket.id],comment=this.comments[ticket.id];if(!status||!comment?.trim()||this.saving())return;this.saving.set(true);try{await firstValueFrom(this.http.put(`/api/v1/support/tickets/${ticket.id}`,{status,assignedTeam:'SUPPORT',comment:comment.trim(),operationId:crypto.randomUUID(),expectedVersion:ticket.version}));delete this.comments[ticket.id];await this.load();}catch(e){this.error.set(apiErrorMessage(e,this.i18n));}finally{this.saving.set(false);}}
async feedback(){if(!this.feedbackMessage.trim()||this.saving())return;this.saving.set(true);try{await firstValueFrom(this.http.post('/api/v1/support/feedback',{type:this.feedbackType,moduleCode:this.moduleCode,message:this.feedbackMessage.trim(),rating:this.feedbackType==='RATING'?this.rating:null,route:location.pathname,applicationVersion:'web',browser:navigator.userAgent,correlationId:crypto.randomUUID(),operationId:crypto.randomUUID()}));this.feedbackMessage='';this.notifications.success(this.i18n.t('support.feedbackSent'));}catch(e){this.error.set(apiErrorMessage(e,this.i18n));}finally{this.saving.set(false);}}
async calculate(){if(this.saving())return;this.saving.set(true);try{this.health.set(await firstValueFrom(this.http.post<Health>('/api/v1/support/health/calculate',{operationId:crypto.randomUUID()})));}catch(e){this.error.set(apiErrorMessage(e,this.i18n));}finally{this.saving.set(false);}}key(prefix:string,value:string){return `${prefix}.${value.toLowerCase().replace('_','')}`;}format(value:number){return value?new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(value):'—';}}
