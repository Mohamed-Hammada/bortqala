[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "universal-access-zkteco"
version = "0.3.0"
description = "Self-contained Zkteco biometric/access integration package"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
