databaseChangeLog:
  # Fully ordered baseline release. The development database is recreated after
  # physical migration reorganization, so these categorized paths are canonical.
  - include:
      file: db/changelog/releases/20260729_v1_v67.changelog-master.yaml

  # New releases are ordered here. Each release may reference focused files from
  # schema/create, schema/update, data/insert, data/update, and data/delete.
  - include:
      file: db/changelog/releases/next.changelog-master.yaml
