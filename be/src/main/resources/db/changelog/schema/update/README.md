# Schema update migrations

Put column alterations, constraint changes, renames, and other modifications to existing schema here.

Never edit an applied migration. Add a new versioned file and include it from the applicable release master.
