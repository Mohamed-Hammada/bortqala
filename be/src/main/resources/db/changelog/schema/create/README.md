# Schema create migrations

Put new table, sequence, index, and constraint creation files here.

Use one focused file per feature/version and include it from the applicable file under `db/changelog/releases/` so cross-folder dependency order remains explicit.
