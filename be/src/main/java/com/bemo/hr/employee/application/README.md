# Employee application / تطبيق الموظفين

**EN:** Transactional use cases, overlap checks, uniqueness rules, optimistic-version checks, and DTO mapping.

The development bootstrap creates an editable DEMO reference configuration from the paper notes. Existing category codes are never overwritten.

يضيف تشغيل التطوير إعدادًا مرجعيًا قابلًا للتعديل لشركة DEMO من ملاحظات الورق، ولا يستبدل أي فئة موجودة بنفس الكود.

**AR:** حالات الاستخدام والمعاملات وفحص تداخل الجداول والتكرار وإصدارات التعديل وتحويل DTO.
