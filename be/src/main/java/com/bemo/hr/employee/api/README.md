# Employee API / واجهات الموظفين

**EN:** Versioned REST contracts under `/api/v1/categories` and `/api/v1/employees`. Entities are never exposed.

**AR:** عقود REST بإصدار ثابت للفئات والموظفين، ولا يتم إرجاع JPA entities مباشرة.
