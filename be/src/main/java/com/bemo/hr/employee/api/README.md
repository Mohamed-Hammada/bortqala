# Employee API / واجهات الموظفين

**EN:** Versioned REST contracts under `/api/v1/categories` and `/api/v1/employees`. Entities are never exposed.

**AR:** عقود REST بإصدار ثابت للفئات والموظفين، ولا يتم إرجاع JPA entities مباشرة.

**EN:** Extended schedule requests include end time and scope while retaining the original six-field constructor for internal callers and focused tests.

**AR:** تتضمن طلبات الجداول الممتدة وقت النهاية والنطاق مع الحفاظ على منشئ الحقول الستة للمتصلين الداخليين والاختبارات المركزة.
