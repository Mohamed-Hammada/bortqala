# Attendance API / واجهة الحضور

**EN:** Upload/list endpoints, unmatched device identities, and live device CRUD/synchronization endpoints under `/api/v1/imports`.

**AR:** رفع وعرض دفعات البصمة والهويات غير المربوطة تحت `/api/v1/imports`.
