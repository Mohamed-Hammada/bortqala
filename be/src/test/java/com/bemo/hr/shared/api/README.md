# JSON date-contract tests / اختبارات عقد التاريخ

**EN:** Proves `LocalDate` and `Instant` are serialized and deserialized only as epoch-millisecond numbers.

**AR:** يتأكد أن `LocalDate` و`Instant` يُرسلان ويُقرآن كأرقام epoch milliseconds فقط.
